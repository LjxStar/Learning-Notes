[计算机组成原理考研笔记六万字整理](https://zhuanlan.zhihu.com/p/1900330013515060981)
====================================================================

共 66800 字阅读需 134 分钟

计算机组成原理考研笔记六万字整理(1)
-------------------

序言
--

笔者在自学计算机并在今年底跨考 408 读研，以下是笔者不断更新的知识整理分享：

[![](https://pica.zhimg.com/v2-ee3146dd8e3bf6740a734fd4db2c99ae.jpg?source=7e7ef6e2&needBackground=1)计算机网络六万字系统整理77 赞同 · 7 评论](https://zhuanlan.zhihu.com/p/679003238) 文章

[![](https://picx.zhimg.com/v2-f45ab34627d03b9c328f84fffa6e2e5b.jpg?source=7e7ef6e2&needBackground=1)数据结构（一）从绪论到各种线性表9 赞同 · 0 评论](https://zhuanlan.zhihu.com/p/679954868) 文章

[![](https://pica.zhimg.com/v2-55c717987a19403db964deec80faf2df.jpg?source=7e7ef6e2&needBackground=1)数据结构（二）从树到图11 赞同 · 0 评论](https://zhuanlan.zhihu.com/p/680012263) 文章

[![](https://pic1.zhimg.com/v2-808a07773367c37bbfe9ba6ddff93195.jpg?source=7e7ef6e2&needBackground=1)六万字漫游操作系统考研知识92 赞同 · 2 评论](https://zhuanlan.zhihu.com/p/682366129) 文章

然后就是到了我个人体感最难学最繁杂最折磨的[计算机组成原理](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BB%84%E6%88%90%E5%8E%9F%E7%90%86&zhida_source=entity)了，笔者本科读的通信工程，学习时相当吃力，于是**内容基于王道考研书以及湖科大的视频**，并**重新编排合理学习内容顺序**，我综合整理了这份知识笔记，便于自己今后复盘和不断更新，相信也对你的学习了解有帮助~

一、计算机组成原理基础通识
-------------

### 1、计算机系统简述

（1）计算机系统组成

**硬件系统和软件系统共同构成完整的计算机系统，具有逻辑功能上的等价性**。

*   硬件是指有形的物理设备，主要由 **CPU、内存、外存、I/O 设备和三总线**组成。
*   软件是指在硬件上运行的程序和相关的数据及文档，分为**系统软件和应用软件**两大类。

冯·诺依曼提出了**[存储程序](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E5%AD%98%E5%82%A8%E7%A8%8B%E5%BA%8F&zhida_source=entity)**的概念，采用**控制流驱动的工作方式**的计算机称为冯·诺依曼机，特点如下：

*   计算机硬件系统由运算器、存储器、控制器、输入设备和输出设备 5 大部件组成。
*   **指令和数据以同等地位存储**在存储器中，均用**二进制**代码表示，但**计算机应能区分**它们。
*   **指令由操作码和地址码组成**，操作码指出操作的类型，地址码指出操作数的地址。

![](https://pica.zhimg.com/v2-ee3bed8fce24632bdeb4307a1dd1a854_r.jpg)

冯·诺依曼机的结构模型

“存储程序”的基本思想是：将事先编制好的**程序和原始数据送入主存储器后才能执行**，一旦程序被启动执行，就无须操作人员的干预，计算机会**按地址访问自动逐条执行指令直至程序执行结束**。

计算机操作系统通常是存储在硬盘上的，但在计算机启动时，通常会将操作系统的一部分代码程序即**内核 kernel 加载到内存中**以便于执行。一旦操作系统启动完成，它会继续在内存中运行，并根据需要从硬盘上加载其他程序和数据到内存中进行处理。

（2）计算机主体硬件简要介绍

**中央处理器 CPU**：计算机中负责执行程序中的指令以及处理各种计算任务，主要包含：

*   **控制单元 CU**（Control Unit）主要负责指令的控制与执行。
*   **算术逻辑单元 ALU**（Arithmetic Logic Unit）处理实际的数学运算和逻辑运算。
*   **寄存器**（Registers）用于临时存储指令、数据或操作结果，是 CPU 内部的高速存储设备。

**内存/主存储器**：计算机用来存储操作系统内核和正在运行的程序数据的地方，可以**被 CPU 直接访问和处理**。它是临时存储器，断电后数据会丢失。

**外存/硬盘**：计算机用来长期存储数据和程序的设备，如硬盘驱动器 HDD、固态硬盘 SSD、光盘等，其数据必须调入内存后才能被 CPU 访问。

**I/O 设备**：用于计算机对外交互，包括：

*   **输入设备**：如键盘、鼠标、触摸屏，它们向计算机输入数据和命令。
*   **输出设备**：如显示器、打印机、扬声器，它们将计算机处理后的结果显示或输出给用户。

**总线 Bus**：连接计算机各部件之间传输数据和信号的通道，可以**减少信息传送线数量**，分为：

*   **地址总线 AB** 指示数据的存储位置。
*   **数据总线 DB** 传输数据。
*   **控制总线 CB** 传输控制信号，如读/写信号、时钟信号等。

![](https://pica.zhimg.com/v2-c5c0dd71347d4f8987efd60d68d7c312_r.jpg)

tips：一般将运算器和控制器集成到同一个芯片上，称为中央处理器 CPU。**CPU 和主存储器共同构成主机**，而除主机外的**其他硬件装置（外存、I/O 设备等）统称外部设备**，简称外设。

（3）计算机软件系统与编程语言

*   **系统软件**是一组保证计算机系统高效、正确运行的基础软件，主要有操作系统 OS、数据库管理系统 DBMS、语言处理程序、分布式软件系统、网络软件系统、标准库程序、服务性程序等。
*   **应用软件**是指用户为解决某个应用领域中的各类问题而编制的程序，如各种科学计算类程序、工程设计类程序、数据统计与处理程序等。

无论是系统软件还是应用软件都是由**编程语言**得到的程序实现的，主要分为：

**[机器语言](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E6%9C%BA%E5%99%A8%E8%AF%AD%E8%A8%80&zhida_source=entity)**（Machine Language）：计算机**硬件唯一可直接理解和执行**的语言，它由二进制代码组成，每个指令和数据都用二进制数表示。不同类型架构的计算机可能有不同的机器语言。

**[汇编语言](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E6%B1%87%E7%BC%96%E8%AF%AD%E8%A8%80&zhida_source=entity)**（Assembly Language）：机器语言的一种**助记符表示形式**，对应不同的机器语言指令集，需要通过汇编器（Assembler）将其转换为机器语言，计算机硬件才能理解和执行，**不同平台之间不可直接移植**。

**[高级语言](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E9%AB%98%E7%BA%A7%E8%AF%AD%E8%A8%80&zhida_source=entity)**（High-Level Language）：更接近人类自然语言，如我们熟知的 C/C++、Python、Java 等语言。我们平时编写高级语言代码，需要通过编译器（Compiler）或解释器（Interpreter）转换为机器语言再被计算机逐行执行，具体生成可执行文件的流程简述如下（以 C 语言代码 GCC 编译举例）：

源代码文件.c \\frac{预处理}{生成} 预处理后的文件.i \\frac{编译}{生成} 汇编语言文件.s \\frac{汇编}{生成} 二进制文件.o \\frac{链接}{生成} 可执行文件.exe

![](https://pic2.zhimg.com/v2-871d0d8ca9a5d5b147aaa69a4bb92cad_r.jpg)

C 语言的源程序转换为可执行文件的过程

tips：**翻译程序分为编译和解释两种**不同的程序执行方式，它们在处理高级语言编写的源代码时有本质区别：

*   **编译器**是将源代码**一次性地转换成机器码或目标代码**的程序，源代码中的语法错误在全都检测后报告，**并生成一个可执行文件**，通常用于编译型语言，如 C/C++、Java，占用空间大。
*   **解释器**是将源代码**逐行或逐块读取并立即执行相应操作**的程序，在运行时解释代码因此启动快运行慢，**不需要生成可执行文件**，通常用于解释型语言，如 Python、Ruby、JavaScript。

（4）计算机性能指标

字长是指计算机一次整数运算所能处理的二进制数据位数，通常为 1 字节 8 位的整数倍。

*   **机器字长**指的是 **CPU 一次整数**运算的数据通路宽度，**等于其 ALU 和通用寄存器宽度**，与总线宽度无必然联系；机器字长越长，数据位数越多，则定点/浮点数**表示及运算的精度越高**。
*   **存储字长**是指**主存单元​**​一次存取操作的位数，**等于数据总线宽度**，缓存行通常含多个存储字；如果存储字长为 32 位，那么每个存储单元可以存储 4 个字节的数据。
*   **指令字长**是指一条指令**在内存中的**​**​编码长度​**​，取决于具体指令集架构，**等于其指令寄存器宽度**，会影响指令的复杂性和指令集的大小。

CPU **寻址空间**是指 CPU 理论可以直接访问的内存范围，**与地址总线宽度**有关，若地址线宽度为 16 位，则其寻址空间为 2^{16} 即 64K。**64 位操作系统**即表示 **CPU 理论寻址空间 64 位但不是实际寻址能力**。

*   **吞吐率**指系统在单位时间处理请求的数量，主要取决于内存读写速度，是**用户评价系统性能的综合参数**。
*   **响应时间**指从用户向计算机发送一个请求开始到获得所需结果的时间，通常**包括 CPU 时间和等待时间**。

**兼容**体现的是**软件或硬件的通用性**，常见于系列机即具有基本相同体系结构，且采用**相同基本指令系统**的多个不同型号计算机。

**实际软件的运行情况可全面代表计算机性能**，而CPU 的性能取决于**主频、CPI 和指令条数**：

*   CPU 时钟周期 T ：机器内部主时钟脉冲信号的宽度，是 CPU 工作的**最小时间单位**。
*   CPU 时钟频率 f ：也称主频，即每秒的时钟周期数， f=\\frac{1}{T} **越大指令执行越快**。
*   CPI 是执行一条指令所需的时钟周期数，**与指令集、架构和计算机组织等有关**，Cache 缺失率影响。
*   IPS 是每秒执行的指令数，有 IPS=\\frac{f}{CPI} ， MIPS =\\frac{f}{CPI\\times 10^6} 是每秒执行百万条指令数。
*   CPU 执行时间：**指令条数 \* CPI \* 时钟周期**。提高 CPU 时钟频率、优化数据通路（提高 CPI）、优化程序编译都能缩短 CPU 执行时间。

tips：相同的高级语言程序在**不同计算机编译生成的指令条数可能不同**；**主频不影响 CPI 仅加速指令执行**。

e.g.假定编译器对高级语言的某条语句可以编译生成两种不同的指令序列，A、B 和 C三类指令的 CPI 和两种不同序列所含的三类指令条数如下表所示，两个指令序列都在时钟周期为 2ns 的机器上运行：

![](https://pic3.zhimg.com/v2-2cf007a1f3768fe71c19fc7034b716c6_r.jpg)

*   序列一 CPI = (1×1 + 1×2 + 4×3)÷6 = 15÷6 = 2.5，MIPS = \\frac{500\\times 10^6}{CPI\\times 10^6}=200 ，执行时间 = 指令条数×CPI×时钟周期 = 15×2ns = 30ns。
*   序列二 CPI = (2×1 + 1×2 + 2×3)÷5 = 10÷5 = 2，MIPS = \\frac{500\\times 10^6}{CPI\\times 10^6}=250 ，执行时间 = 指令条数×CPI×时钟周期 = 10×2ns = 20ns。

### 2、从触发器到存储器

**触发器**（Flip-Flop）是计算机记忆装置的基本单元，由逻辑门电路构成且具有特定的时序特性，一个触发器只能存储一位二进制数值（0或1）。**一个触发器可以被看作是一个一位寄存器**。

![](https://pic3.zhimg.com/v2-765752a96f7964694ca2167769ed044e_r.jpg)

基本 RS 触发器结构

**寄存器**（Register）是一种用于存储一或多位二进制数据的存储器件，由一或多个触发器构成，常用于存储 CPU 中的操作数、地址、状态信息等。**多个寄存器可以构成一个存储器**。CPU 包含的有：

*   **通用寄存器 GPR**：累加 ACC、乘商 MQ、操作数寄存器 X、变址寄存器 IX、基址寄存器 BR 等。
*   **主存地址寄存器 MAR**：存放将要访问的主存单元的地址，**位数与地址码长度相同**，16 位表示有 2^{16} 个可寻址的存储单元即 64 KB 内存。
*   **主存数据寄存器 MDR**：存放从主存中读取的数据或将要写入主存的数据，**位数与存储字长相同**，32 位表示每次读写 32 位数据​。
*   **指令寄存器 IR**：存放**当前执行的指令代码**，位数取决于**指令字长**。
*   **程序计数器 PC**：存放**下一条指令的地址**，位数取决于**存储器字数和指令字长**，具有**自增**功能，计算机按字编址则每次 +1，按**字节编址则自增指令占用字节数**。
*   **程序状态寄存器 PSW**：存放标志信息。

**存储器**（Memory）是计算机中用于存储数据和程序的设备，通常由多个**相同位数的存储单元**构成，​​每个存储单元通常存放一个存储字，且**存储容量 = 存储单元数量 × 每单元存储数据量**（如 64K × 1 字节 = 64KB）。

tips：**相联存储器**既可以按地址寻址又可以按内容寻址。

（2）计算机存储地址认识

起始地址指的是**程序或数据载入内存时的首地址**，通常由 OS 决定并分配位置，通常以十六进制表示。在[分段式内存管理](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E5%88%86%E6%AE%B5%E5%BC%8F%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86&zhida_source=entity)中，起始地址可能是一个段的起始位置即基址。

逻辑地址是**内部和编程使用的虚拟地址**，通常由 CPU 编译程序中的代码指令生成。在分段机制中，逻辑地址由**段号**和**偏移量**组成；在分页机制中，逻辑地址由**页号**和**页内偏移量**组成。逻辑地址 \\frac{地址转换机制}{如分段、分页} 物理地址，然后被发送到内存模块进行访问。

物理地址是处理器通过地址总线直接发送到内存芯片以访问内存单元的地址。它是实际的内存位置，是由**内存管理单元（MMU）将逻辑地址转换为的真实地址**，用于在内存中唯一标识一个存储单元。

*   基址是指在**分段式**内存管理中表示**段的起始地址**。它是一个相对于段寄存器所指定的段偏移地址的值，通常也以十六进制表示。
*   偏移地址是在分段式内存管理中**相对于基址的一个偏移量**，用于定位段内的具体位置。程序或数据的实际地址由基址和偏移地址共同确定。

tips：**分段式里物理地址 = 基址 + 偏移地址**，如基址 0x8000，偏移地址 0x1000，则物理地址为 0x9000；分页式里物理地址通过页表查询，这块内容详见操作系统部分~

二、数据的表示和运算
----------

### 1、数制与编码

（1）四种主要数制

二进制 B（Binary）：基数为 2，逢 2 进位，使用数字 0 和 1 表示数值，以前缀"0b"或"0B"开头，如 0b1010、0B1101 等。在计算机内部，所有**数据都以二进制形式存储和处理**。

八进制 O（Octal）：基数为 8，逢 8 进位，使用数字 0 到 7 表示数值，以前缀"0"开头，如 001、075 等。在计算机领域一般用于表示**文件权限、位操作和硬件寄存器地址**。

十进制 D（Decimal）：基数为 10，逢 10 进位，使用数字 0 到 9 表示数值，是我们**日常生活中最常用的计数系统**，一般用于表示时间、货币、数量等。

十六进制 H（Hexadacimal）：基数为 16，逢 16 进位，使用数字 0 到 9 和字母 A 到 F 表示数值，其中 A 到 F 分别代表十进制的 10 到 15，以前缀"0x"或"0X"开头，如 0x1A、0xB3 等。在计算机编程和相关领域广泛使用，一般用于表示**内存地址、颜色值、ASCII 字符**等。

相互转换规则：二转八（每**三位二进制数**可以转换为一个八进制数）、二转十（将**二进制数按权展开并求和**即可转换为十进制数）、二转十六（每**四位二进制数**可以转换为一个十六进制数）。其余同理变换。

![](https://picx.zhimg.com/v2-33d662e7df103823cc062e0e8805a54b_r.jpg)

二进制数 1111000010.01101 分别转换为八进制数 1702.32 和十六进制数 3C2.68

![](https://pic2.zhimg.com/v2-6ae5c91ebd41b3bf753dc7e4940277b7_r.jpg)

十进制数 123.6875 转换成二进制数 1111011.1011，整数部分从下到上，小数部分从上到下

tips：计算机系统内部采用二进制编码所有数据信息，在字长足够时**可以表示任何一个整数**，但**二进制小数无法表示所有的十进制小数，只能无限接近**。N 位二进制小数共有 \\(2^N\\) 种状态表示不同的小数，而十进制小数能表示的数的个数为 \\(10^N\\) ，二者的商为 \\((0.2)^N\\) 。

（2）常见码制

**8421BCD 码**：即 Binary-Coded Decimal，将十进制数的每个位都展开为 4 位二进制数。示例如下：

63(D)=0110 0011(BCD) 127.25(D)=0001 0010 0111.0010 0101(BCD)

**ASCII 码**：美国信息交换标准码，参考图例如下：

![](https://pic1.zhimg.com/v2-8ef5e803831267b1b1f31b53030e4018_r.jpg)

**[格雷码](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E6%A0%BC%E9%9B%B7%E7%A0%81&zhida_source=entity)**：相邻两个码之间只有一位不同的无权循环码。

二进制转格雷码：格雷码的最高位（左边）与二进制码最高位相同，从左到右逐一将二进制码相邻两位相加（不考虑进位）作为格雷码的下一位。示例如下：

![](https://pic1.zhimg.com/v2-2bb4b8e2de09e5a6730e11d3025d503c_r.jpg)

格雷码转二进制码：二进制码的最高位（左边）与格雷码的最高位相同，产生的每一位二进制码与下一位相邻的格雷码相加（不考虑进位）作为二进制码下一位。示例如下：

![](https://pic1.zhimg.com/v2-9e5de380d8621ebbbd95be83d94d77fc_r.jpg)

**[Unicode码](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=Unicode%E7%A0%81&zhida_source=entity)**：一种字符编码标准，使用固定的代码点表示字符，可以表示世界上几乎所有的字符集，它有 UTF-8、UTF-16 和 UTF-32 等多种实现方式，UTF 即 **Unicode Transformation Format**。

*   UTF-8：互联网上最常用的字符编码方式，一种**可变长度**字符编码方案，使用 1 到 4 个字节表示 Unicode 字符集中的所有字符，对于 ASCII 字符，使用 1 个字节表示，其余字符使用多个字节表示。
*   UTF-16：一种**固定长度**字符编码方案，可以表示基本多语言平面（BMP）中的所有字符，对于非 BMP 字符，使用一对代理对来表示。UTF-16 在一些基于 Windows 的系统中广泛使用。
*   UTF-32：使用**固定四个字节**来表示每个 Unicode 字符，常用于要求高效随机访问和字符处理的应用。

tips：英文字符的编码为**1字节编码**，汉字字符的编码为**2字节编码**。

（3）计算机数值的表示方式

**真值**：即我们在数学运算中实际使用的数值，例如 +3.14159 是一个真值。

在计算机内部，数值数据的表示方法有以下两大类：

*   直接用二进制数表示：有符号数分为**[定点数](https://zhida.zhihu.com/search?content_id=257124313&content_type=Article&match_order=1&q=%E5%AE%9A%E7%82%B9%E6%95%B0&zhida_source=entity)**表示和**浮点数**表示；无符号数用来表示**地址**等无符号整数。
*   二进制编码的十进制数：一般采用 **BCD 码用来表示整数**。 因此计算机中的数值数据虽然都用二进制表示，但也有用十进制表示的，例如在指令类型中存在十进制加法指令。

**机器数**：计算机中实际存储和处理的**二进制数值**，根据**小数点位置是否固定**，分为定点数和浮点数。

*   **定点数**：小数部分位数固定的数值，同字长下**精度更大**，运算**超过数的表示范围**即溢出。
*   **浮点数**：运算需要规格化较为复杂，同字长下**范围更大**，规格化后**阶码超出范围**时溢出。

机器数包括数值本身和一些额外的信息，如符号位（表示数值的正负）和指数位（对于浮点数）；定点数和浮点数都可以是有符号的或无符号的。

在计算机中，由于存储和处理能力的限制，**真值通常需要转换为机器数（定点数或浮点数）来进行存储和运算**。这种转换可能会导致数值的舍入或截断，从而引入误差。

（4）数据的大小端和对齐存储

在存储数据时，数据从低位到高位可以按从左到右排列，也可以按从右到左排列。因此通常用最低有效字节（LSB）和最高有效字节（MSB）来分别表示数据的低位和高位。例如在 32 位计算机中，一个 int 型变量 i 的机器数为01 23 45 67H，其 **MSB = 01H，LSB = 67H**。

现代计算机基本都采用**字节编址**，即**每个地址编号中存放 1 字节**。不同类型的数据占用的字节数不同，如 int 型和 float 型占 4 字节，double 型占 8 字节等，而程序中对**每个数据只给定一个地址**。根据数据中各字节在连续字节序列中的排列顺序不同，可以采用两种排列方式：

![](https://pic3.zhimg.com/v2-1b9737c23511edf9981507ec0931b14c_r.jpg)

大端 big endian 先高位后低位存储；小端 little endian 先低位后高位存储

假设字长为 32 位，数据按边界对齐方式存放要求其**存储地址是字节长度的整数倍**，这样无论所取的数据是字节、半字还是字，均可一次访存取出。当所存数据不满足上述要求时，可通过填充空白字节使其符合要求。这样做虽然会浪费一些存储空间，但可以**提高存取数据的速度**。

![](https://picx.zhimg.com/v2-802ba332802bb6bbf85e9bb5c32318fb_r.jpg)

按边界对齐方式存储和按边界不对齐方式存储

tips：C 语言中的 struct 类型边界对齐需要：

*   **每个成员按其类型的大小对齐**，char 型的对齐值为 1，int 型的对齐值为 4，单位为字节；
*   struct 的长度必须是成员中**最大对齐值的整数倍**。

```
struct info {
    char id;     // 1B
    short phone; // 2B
    int post;    // 4B
} x;
```

struct info的长度是 int 的 4B 整数倍，因此 id **占 1B 后面一字节留空来对齐**。

### 2、定点数的表示与运算

（1）数值的表示与转换

数学中带“+/-”符号的数称为**真值**，而在计算机中通常将数的符号和数值部分一起编码，将数的符号数字化为 0 正 1 负，这种有符号整数称为**机器数**，常用的有原码、反码、补码和移码，真值 -0.1101 即原码 1.1101，它们可以实现带符号数的加减法运算**实现带符号数的加减法运算**。

*   **原码**：最高位（左边）为符号位，如 +5 原码为 00000101，-5 原码为 10000101。
*   **反码**：正数的反码同原码，负数的反码将其原码**除符号位外全部取反**，如 -5 反码为 11111010。
*   **补码**：正数的补码同原码，负数的补码是在其**反码的基础上加 1**，如 -5 补码为 11111011。
*   **移码**：真值加上偏置值，可直接由其**补码符号位取反得到**，只用于**定点整数**的表示，常用于**浮点数的阶码**。

![](https://pic2.zhimg.com/v2-4ca29d6251f2d1296ac55d088b119b91_r.jpg)

真值 x 为正数时转换简单

![](https://pic2.zhimg.com/v2-9da4b2160de000d95ab46d11eb315217_r.jpg)

真值 x 为负数时转换复杂，求补码的负数是连同符号位一起取反再加 1

*   tips：n 位**无符号整数**的范围为 0 ~ 2^n-1 ；
*   **有符号整数**原码、反码的范围为 -(2^{n-1}-1) ~ 2^{n-1}-1 ，**补码、移码的范围为** \- 2^{n-1} ~ 2^{n-1}-1 。

![](https://picx.zhimg.com/v2-10afd0a9da9aaf2bb9b4c53d888bc1dd_r.jpg)

补码符号位取 1，数值位全 0 则得到最小值

![](https://pic1.zhimg.com/v2-143cec88f4205bcb5818ea6ec7aadb9e_r.jpg)

0 的补码和移码表示唯一但不同；负数补码的数值位越小，真值越小，绝对值越大

原码、反码和补码都可以用于实现带符号数的加减法运算，但**补码**是最常用的，因为它**简化了硬件设计**，使得**加法和减法可以通过相同的电路实现**，也解决了原码和反码在表示 0 时的二义性问题。

e.g.令 A=+15=+00011111，B=+24=+00110000 可得补码 \[A\] = 00001111，\[B\] = 00011000，求得 \[−B\] = 11101000。所以补码加减运算：

*   \[A+B\] = \[A\] + \[B\] = 00001111 + 00011000 = 00100111，符号位为 0，对应真值为 +39。
*   \[A−B\] = \[A\] + \[−B\] = 00001111 + 11101000 = 11101111，符号位为 1，对应真值为 -9。

e.g.设 x 为整数，其中 \\(x\_i\\) 取 0 或 1：

*   若 \\(\[x\]\_{\\text{补}} = 1,x\_1x\_2x\_3x\_4x\_5\\) ，要 \\(x < -16\\) ，则 \\(x\_1\\) 必须为 0， \\(x\_2 \\sim x\_5\\) 任意。
*   若 \\(\[x\]\_{\\text{补}} = 1,x\_1x\_2x\_3x\_4x\_5x\_6\\) ，要 \\(x > -32\\) ，则 \\(x\_1\\) 必须为 1， \\(x\_2 \\sim x\_5\\) 至少有一位 1。

e.g.一个 8 位的二进制整数由 2 个“0”和 6 个“1”组成，采用移码表示：

*   移码的定义为： \[x\]\_{\\text{移}} = 偏置值 + x 真值（即要表示的整数）。
*   若偏置值为 127 让此整数最小，需移码 \\(\[x\]\_{\\text{移}}\\) 最小 00111111 转十进制 63，x = 63-127 = -64。
*   若偏置值为 128 让此整数最大，需移码 \\(\[x\]\_{\\text{移}}\\) 最大 11111100 转十进制 252，x = 252-128 = 124。

（2）定点整数与定点小数的表示

在现代计算机中用寄存器存储具体数值，一般用**二进制补码表示有符号定点整数**，用二进制原码或二进制补码表示定点小数，用**无符号数表示主存地址**。

![](https://pica.zhimg.com/v2-9de3f999bebee5c4ae5aef5392e8c8a8_r.jpg)

机器字长 n+1 位定点整数与定点小数（纯小数）在计算机中的表示形式

![](https://picx.zhimg.com/v2-0973b8a1647466cb0ce2dab2dfd1873d_r.jpg)

（3）C 语言的整数类型与转换

C 语言若不指定 signed/unsigned 时**默认为有符号整数**，字符型 8 位 **char 默认无符号**，取决于编译器。

*   **有符号**整数：可分为短整型 16 位 short、整型 32 位 int、长整型 32/64 位 long。
*   **无符号**整数：只需在 short/int/long 之前加上关键字 unsigned，如无符号整型 unsigned int。

上述类型都是按**补码形式存储**的，只是有符号整数的最高位代表符号，而无符号整数的全部二进制位都表示数值，没有符号位，相当于数的绝对值，因此两者所表示的数据范围有所不同。

![](https://pic2.zhimg.com/v2-ba67196b5d558aea153ce4317491d87b_r.jpg)

相同字长不同类型之间的强制转换

强制类型转换的结果是**保持二进制各位的位值不变，仅改变解释这些位的方式**：

执行 short x = -4321, unsigned short y = (unsigned short) x; 可以得到 y = 61215。

*   4321 的二进制是 1000011100001（13 位），补全到 16 位即 0001000011100001。
*   ​​-4321 的二进制原码 1001000011100001，补码则是 1110111100011111 转换不变。
*   无符号解释​​ 2^{15} + 2^{14} + 2^{13} + 2^{11} + 2^{10} + 2^9 + 2^8 + 2^4 + 2^3 + 2^2 + 2^1 + 2^0 = 61225 。

![](https://pica.zhimg.com/v2-ca47ac394bf18d0241cb3e3459f1111c_r.jpg)

两个变量对应的每位都是相同的

同理 unsigned short x = 65535, short y = (short)x; 即 16 位全 1 的 x 解释为有符号 y = -1。

涉及不同字长整数的运算时，分为以下两种情况：

*   大字长向小字长转换要把**多余的高位部分截断**然后赋值低位部分，范围缩小且很可能出错。
*   小字长向大字长转换则**负数用 1、正数用 0** 填充附加位，**无符号数扩展用 0 填充**附加位。

![](https://pic4.zhimg.com/v2-6d28927160d3cf19321327078b2ef985_r.jpg)

截断高位仅保留低位字节，s 和 us 正数直接转换，c 和 uc 负数在有符号和无符号解释有差别

e.g.16 位补码整数 0x8FA0 扩展为 32 位，由于该数是负数即用 1 填充，得到 0xFFFF8FA0。

有符号数和无符号数一起参与运算时计算机**按无符号数来解释最终执行结果**。

![](https://pic2.zhimg.com/v2-5fe121adf2eed83aa384c07f4c78f227_r.jpg)

大字长转小字长会发生截断从而导致数值发生变化，可能产生错误

![](https://pic1.zhimg.com/v2-5cb6252aa20bb011baeffa965401abb0_r.jpg)

（4）加法器

在计算机中，运算器由算术逻辑单元 ALU、移位器、状态寄存器 PSW 和通用寄存器组等组成，基本功能包括加、减、乘、除四则运算，与、或、非、异或等逻辑运算，以及移位、求补等操作。ALU 属于**组合逻辑**电路，其核心部件是**加法器**。

全加器 FA 是最基本的加法单元，输入有加数 A\_i, B\_i 和低位进位的 C\_{i-1} ，输出有本位和 S\_i 和进位 C\_i ：

求和： S\_i=A\_i\\oplus B\_i\\oplus C\_{i-1} ；进位 C\_i=A\_iB\_i+(A\_i\\oplus B\_i)C\_{i-1} 。

将 n 个全加器相连得到 n 位加法器称为串行进位加法器，每级进位直接依赖于前一级的进位，由于**位数有限高位自动丢失**，实际上是模 2^n 的加法运算。

![](https://pic3.zhimg.com/v2-94edf821f344ef734a3112b8c1c6f536_r.jpg)

n 位串行加法器

对于 n 位加法器，除了运算结果，还要关注加法运算是否发生溢出、运算结果的正负性、运算结果是否为 0 等。因此，需要在 n 位加法器的基础上增加一些逻辑电路，使其还能生成相应的各占一位的标志信息：

*   OF=C\_n\\oplus C\_{n-1} 溢出标志表示**有符号数** 0 无 1 有溢出，即**符号位进位与最高数值位的异或**结果。
*   CF=C\_{out}\\oplus C\_{in} 进位/借位标志表示**无符号数** 0 无 1 有溢出，即 **Sub 信号与最高位的异或**结果。
*   SF=F\_{n-1} 符号标志表示**有符号数**运算结果 0 正 1 负；
*   ZF 零标志表示**有/无符号数**运算结果是否为 0，0 否 1 是。

**运算器本身无法识别**所处理的二进制串是有符号数还是无符号数，可通过**标志信息**来区分运算结果。

**无符号数的大小**比较以执行 A - B 为例，通过 ZF 和 CF 判断：

*   若 \\(A = B\\) ： \\(A - B = 0\\) ，则 ZF=1 ，无借位 CF=0 。
*   若 \\(A > B\\) ： \\(A - B\\) 结果非零，且无借位（ ZF=0,CF=0 ）。
*   若 \\(A < B\\) ： \\(A - B\\) 结果非零，且有借位（ ZF=0,CF=1 ）。

**有符号数的大小**比较以执行 \\(\[A\]\_{\\text{补}} - \[B\]\_{\\text{补}}\\) 为例，通过 ZF 、 OF 、 SF 判断：

*   若 A = B：结果为 0，则 ZF=1。
*   若 \\(A > B\\) ： ZF=0 且 OF=SF ；没溢出则两者均为 0，发生溢出则两者均为 1。
*   若 \\(A < B\\) ： ZF=0 且 OF\\ne SF ；没溢出则前 0 后 1，发生溢出则前 1 后 0。
*   两个数最高位相减不够减，则需借位 CF=1，即 A 正数 B 负数情况下。

![](https://pic3.zhimg.com/v2-609a2a3085507f6b8a1c8043ea8e2a4e_r.jpg)

对于**带进位的加减法器**我们**不区分两数是否有符号**，下面详细拆解来理解它的工作原理：

*   ​多路选择器​​： 负责根据 Sub 信号，选择输入是 Y 还是按位取反 \\bar{Y} ​​，输出为 Y' 。
*   加法器​​： 负责对输入 X,Y' 进行​​二进制加法运算​​，产生运算结果 F 及各种标志 ZF、OF、SF、CF。
*   通常 Sub = 0 时，执行加法​​运算；Sub = 1 时，执行减法​​运算。
*   OF=C\_n\\oplus C\_{n-1} **有符号数** 0 无 1 有溢出，两数**不同符号相加、同符号相减都不会溢出**。
*   CF=C\_{out}\\oplus Sub **无符号数** 0 无 1 有溢出；
*   SF=F\_{n-1} **有符号数**运算结果 0 正 1 负；
*   ZF **有/无符号数**运算结果是否为 0，0 否 1 是。

![](https://picx.zhimg.com/v2-da03f63aaaa8dcc5c66c3d4cf458fe61_r.jpg)

tips：可以用**一个 n 位加法器**辅助电路实现**有/无符号整数的加减运算**：

**无符号整数**：可实现模 \\(2^n\\) 加法，减法可通过加补数实现。

**有符号整数**： \\(\[a + b\]\_{\\text{补}} = \[a\]\_{\\text{补}} + \[b\]\_{\\text{补}} \\ (\\text{mod}\\ 2^n)\\) 、 \\(\[a - b\]\_{\\text{补}} = \[a\]\_{\\text{补}} + \[-b\]\_{\\text{补}} \\ (\\text{mod}\\ 2^n)\\) 。

（5）算术逻辑单元 ALU

ALU 核心是**带标志加法器**，也能执行与或非和移位等操作，**ALUop 的位数 n** 决定其最多 2^n 种操作。

![](https://picx.zhimg.com/v2-b8fb0000b8b2b996e882a42338877d35_r.jpg)

ALU 基本结构图，位宽同机器字长

无乘除法电路时，编译器可以通过**在循环代码段中通过比较、加法、移位等指令实现乘法运算**。此时控制逻辑的作用为**制循环次数，控制加法和移位**操作。

*   **逻辑移位**将操作数视为**无符号整数**，规则：左移时，高位移出，低位补 0，若**移出 1** 则发生溢出；右移时，低位移出，高位补 0。
*   **算术移位**将操作数视为**有符号整数**，规则：左移时，高位移出，低位补 0，若前后的**符号位变化**则发生溢出，低位补 0；右移时高位**补符号位**，若低位的 ​**​1 移出会影响精度​**。​

e.g.假定有两个整数用 8 位补码分别表示为 \\(r\_1 = \\text{F5H}\\) ， \\(r\_2 = \\text{EEH}\\)，转换为**真值即 -11 和 -18**，若将运算结果存放在一个 8 位寄存器中，**8 位补码的表示范围为 \[-128, 127\]**， \\(r\_1 \\times r\_2\\) 的结果为 198 发生溢出。

e.g.某计算机字长为 8 位，CPU 中有一个 8 位加法器。已知无符号数 x = 69，y = 38，若在该加法器中计算 x - y，求加法器的两个输入端信息和输入的低位进位信息。

不管是补码减法，还是无符号数减法，都是用**被减数加上减数的负数的补码**来实现的。减数 y 的负数的补码 \\(\[-y\]\_{\\text{补}} = \\bar{Y} + 1\\) ，因此，在加法器的 \\(Y'\\) 输入端用一个反向器实现，并用控制端 \\(\\text{Sub}\\) 控制多路选择器是否将 y 的各位取反后，输入 \\(Y'\\) 端，同时将 \\(\\text{Sub}\\) 作为低位进位送到加法器。当 \\(\\text{Sub}\\) 为 1 时将 \\(\\bar{Y}\\) 输入加法器 \\(Y'\\) 端，即实现各位取反功能；同时将 \\(\\text{Sub} = 1\\) 作为低位进位送到加法器，实现末位加 1 功能。

tips：判断补码运算是否溢出，主要依据**符号位的变化**或**运算前后数值的范围**：

*   补码加减法：同号相加，**结果异号**则溢出。
*   进位规则：若加法器**次高位的进位**与**最高位的进位**不同，则溢出。
*   左移加减法：左移后**符号变**，则发生溢出。
*   右移加减法：右移丢 1 则失精度，一般**不溢出**。

补码定点数加减运算溢出判断有三种方式，均需要**溢出判别电路**，可以用**异或门**实现：

*   单符号位： V=A\_SB\_S\\bar{S\_S}+\\bar{A\_S}\\bar{B\_S}S\_S 为 0 无溢出，为 1 溢出。
*   双符号位： V=S\_{S1}\\oplus S\_{S2} 为 0 无溢出，为 1 溢出，正溢出为 01，负溢出为 10，正结果 00，负结果 11；这种**模 4 补码只需一个符号位**更易检查溢出，只在 **ALU 中使用双符号位**。
*   进位判断： V=C\_n\\oplus C\_{n-1} 为 0 无溢出，为 1 溢出。

（6）乘除法运算电路

原码乘法运算中把**符号位与数值位分开处理**，数值部分是两数**绝对值乘积**，符号不参加运算取两者**符号位的异或**。乘数 Y 的每一位 \\(y\_i\\) 决定是否将被乘数 X 加到部分积上，然后部分积右移一位：

*   **乘数第 4 位** \\(y\_4 = 1\\) ：部分积初始为 0 加 X 得到 0.1101 再右移 1 位，变为 0.01101。
*   **乘数第 3 位** \\(y\_3 = 1\\) ：部分积 0.01101 加 X 得到 1.00111 再右移 1 位，变为 0.100111。
*   **乘数第 2 位** \\(y\_2 = 0\\) ：不加 X 部分积仍为 0.100111，右移 1 位，变为 0.0100111。
*   **乘数第 1 位** \\(y\_1 = 1\\) ：部分积 0.0100111 加 X 得到 1.0001111 再右移 1 位，变为 0.10001111。

原码除法运算也把**符号位与数值位分开处理**，数值部分是两数**绝对值之商**，符号不参加运算取两者**符号位的异或**。被除数减去除数，若够减则商 1，否则商 0 并恢复余数，然后左移继续试除：

*   **第一次试除**：初始 1111 减除数 0010，得到**正余数** 1101 左移 1 位即 1010，商第 1 位为 1。
*   **第二次试除**：余数 1010 减除数 0010，得到**正余数** 1000 左移 1 位即 0000，商第 2 位为 1。
*   **第三次试除**：余数 0000 减除数 0010 为负，恢复余数再左移 1 位即 0000，商第 3 位为 0。
*   **第四次试除**：余数 0000 减除数 0010 为负，恢复余数再左移 1 位即 0000，商第 4 位为 0。

![](https://pic3.zhimg.com/v2-c0202be9f92a271c85a4940cf813385a_r.jpg)

手算乘除法步骤，其中的右移为逻辑右移，这是计算机内部运算过程

在字长为 32 位的计算机中，对于两个 int 型变量 x 和 y 的乘积，若**乘积高 32 位的每一位都相同**，且都**等于乘积低 32 位的符号**，则表示不溢出，否则表示溢出。当 x 和 y 都为 unsigned int 型变量时，若**乘积的高 32 位全为 0**，则表示不溢出，否则表示溢出。

计算机组成原理考研笔记六万字整理(2)
-------------------

![](https://pic4.zhimg.com/v2-a9035ea62bb6fbcd706e6e2cdc055bc9_r.jpg)

32 位无符号数乘法运算的逻辑结构图

![](https://pic4.zhimg.com/v2-ce62eadd3fa0bf68f45bd487ecf82b63_r.jpg)

32 位除法运算的逻辑结构图

tips：用 **ALU 和 SR 实现**的乘法（通用硬件实现）只需要**一条乘法指令但需要多个时钟周期**完成；使用**阵列乘法器**实现的乘法（专用硬件实现）只需要**一条乘法指令且只需一个时钟周期**完成。

### 3、浮点数的表示与运算

（1）浮点数的表示

浮点数表示法是指**以适当的形式将比例因子表示在数据中**，让小数点的位置根据需要而浮动，可以既扩大了数的表示范围，又保持了数的有效精度。表示为 N=(-1)^S\\times M\\times R^E ，S 取值 0 或 1 来决定符号。

![](https://pic1.zhimg.com/v2-6fb69dcd894a4ff6ae911f53944c7d6a_r.jpg)

尾数 M 一般用原码小数表示，阶码 E 一般用移码表示同样也是 R 进制，基数 R 通常隐含表示

阶码可以用**原码、反码、补码、移码**表示，尾数可以用**原码、反码、补码**表示，对应表示范围会不同。

![](https://pic2.zhimg.com/v2-d8ab90c1f8eb9171bb4efa97e547f13b_r.jpg)

一个 32 位短浮点数的存储格式，阶码 E 偏置值为 64，阶码的值反映小数点的实际位置

等长情况下，**基数越大范围越大且精度越低**，**阶码的位数反映浮点数表示范围**，**尾数的位数反映其精度**。

![](https://picx.zhimg.com/v2-0e4f100f588d5a4b266384c9e159bbc7_r.jpg)

阶码与尾数的最值关系对整个浮点数的最值关系影响

原码是关于原点对称的，因此浮点数的**范围也是关于原点对称**，且由于机器字长限制，浮点数仍存在溢出现象，在正负数区域但不在某个数轴刻度上时也可能有精度溢出问题，此时只能以近似数表示。

*   **上溢**：运算结果绝对值大于机器可表示最大绝对值。此时机器**必须中断运算，进行溢出处理**。
*   **下溢**：运算结果绝对值小于机器可表示最小绝对值。此时将其**按机器 0 强制处理，继续运行**。

**规格化**：二进制浮点数要求其**尾数最高位为 1**，使其表示**形式唯一**、**精度最高**。

*   **原码**规格化表示是正数 0.1xxx……，负数 1.1xxx……，**尾数第一位为 1**。
*   **补码**规格化表示是正数 0.1xxx……，负数 1.0xxx……，**尾数第一位与符号位不同**。
*   **左规**：当运算结果的**尾数的最高数位不是有效位时**使用，可能要进行**多次**。
*   **右规**：当运算结果的**尾数的有效位进到小数点前面时**使用，只需进行**一次**。

![](https://pic1.zhimg.com/v2-8ead60109086258d9487f8000eabb554_r.jpg)

![](https://pic2.zhimg.com/v2-1f934cf14eccfcc233ba76bf3cdd2b2f_r.jpg)

二进制浮点数 110.0101 的规格化过程

![](https://picx.zhimg.com/v2-8b447a3ce82f006e8e465fd5e5c1b857_r.jpg)

e.g.设十进制数 -56，要求浮点数字长 16 位（阶码 5 位尾数 11 位），则其二进制形式：-111000

![](https://picx.zhimg.com/v2-a1497573988a137f532e643d9bc879a1_r.jpg)

同基数的规格化正浮点数，阶码越大则浮点数值一定越大

（2）IEEE 754 标准认识

IEEE 754 标准主要包括以下两种浮点数格式：32 位单精度和 64 位双精度对应 C 中的 float/double。

![](https://pic1.zhimg.com/v2-d08987a4a1a2fa74e21f913eae33f930_r.jpg)

符号 0 正 1 负，阶码定点整数用移码表示，尾数定点小数用原码表示

![](https://pic4.zhimg.com/v2-82689036aaa986b6d4ed713aa2a312b1_r.jpg)

对于**规格化**的二进制浮点数，**尾数的最高位总是 1**，通常作为**隐藏位**在小数点之前，使得尾数可以多表示一位有效位。如 (12)\_{10}=(1100)\_{2}=1.1\\times 2^3 整数部分的 1 不存储在尾数之中。

注意偏置值是 2^{n-1}-1，在存储浮点数阶码之前，偏置值要先加到阶码真值上，如 float 单精度中 E 是 8 位移码**取值范围 1-254 避免全 0/1 特殊值**。上例中，阶码值为 3，因此

*   在单精度浮点数中，移码表示的阶码为 127 + 3 = 130(82H)；
*   在双精度浮点数中，移码表示的阶码为 1023 + 3 = 1026(402H)。

![](https://pic2.zhimg.com/v2-81eb04b89ca552462629ffce68cfbf95_r.jpg)

采用偏置值 128 的最小规格化数的倒数会发生溢出，用 127 时取值在 -126~127 之间未溢出

tips：IEEE 754 标准中**阶码全 0 尾数不全为 0** 表示**非规格化数**，此时**隐藏尾数位是 0 不是 1**，单精度 32 位取值为 \\pm 2^{-126}\\times 0.f ，可用于处理**阶码下溢**。**阶码全 1 尾数全 0 表示无穷大的非数 NaN**。

*   float 类型取最大整数 \\((2 - 2^{-23}) \\times 2^{127} = 2^{128} - 2^{104}\\) ，最小整数即 \\(-(2^{128} - 2^{104})\\) 。
*   double 类型取最大整数 \\((2 - 2^{-52}) \\times 2^{1023} = 2^{1024} - 2^{971}\\) ，最小即 \-( 2^{1024} - 2^{971}) 。

e.g.若某单精度浮点数、某原码、某补码、某移码的 32 位机器数均为 0xF0000000，求这些数从大到小的顺序。

这个机器数的**最高位为 1**，对于原码、补码、单精度浮点数而言为负数，**对于移码而言为正数**，所以移码最大，而补码为 \\(-2^{28}\\) ，原码为 \\(-(2^{30} + 2^{29} + 2^{28})\\) ，单精度浮点数阶码为 2^7+2^6+2^5-127=97 ，即数值为\\(-1.0×2^{97}\\) 。它们大小依次递减。

十进制数转 IEEE 754 单精度浮点数十六进制机器码：

![](https://picx.zhimg.com/v2-d93f62e291cbe3fab485818ebed9fbff_r.jpg)

对应十六进制机器码为 43CC5800H

![](https://pica.zhimg.com/v2-17545f7c2872036d41c8de2bf99f362e_r.jpg)

对应十六进制机器码为 BEE00000H

IEEE 754 单精度浮点数十六进制机器码转十进制真值：

![](https://pic3.zhimg.com/v2-3e347f0089b8cedba5630e3f0f5ab86c_r.jpg)

对应浮点数的真值为 -16.375

![](https://pica.zhimg.com/v2-a2634b9f5ec813dd82d278e5d0c1f870_r.jpg)

![](https://pic3.zhimg.com/v2-b6eca0a4dc57532401bd6d324f11ce7a_r.jpg)

（3）浮点数的加减运算

浮点数运算的特点是**阶码和尾数分开运算**，分为以下步骤：

**对阶**：使得两个操作数的小数点位置对齐即阶码相等，将**小阶码增大**到与大阶码一致，**尾数右移**。

![](https://pic1.zhimg.com/v2-d6b13996950f24fa28fb6c0649f02ec8_r.jpg)

右移时低位移出的位保留并参与尾数部分的运算，结果规格化后再舍入，这过程不会发生溢出

**尾数运算**：按定点小数的补码加减法进行运算，IEEE 754 浮点数还有将隐藏位还原到尾数部分。

![](https://pica.zhimg.com/v2-43f585fc0ca085f23be0a7bac78c34c0_r.jpg)

**结果规格化**：

![](https://picx.zhimg.com/v2-9ace9a6d0940a5ee1c68311e0d91c86b_r.jpg)

**舍入**：**仅浮点数**使用的概念，只有**对阶和右规**两种情况，**不一定产生误差**。常用有以下方法：

![](https://pic2.zhimg.com/v2-1a63d8bce123b004dcfe4c0f8dd0c055_r.jpg)

**溢出判断**：正指数超过最大允许值则是指数上溢，负指数超过最小允许值则是指数下溢，判定即**阶码是否上溢**。**右规和尾数舍入过程可能上溢**，**尾数溢出时可能产生误差但不一定溢出**，且可通过右规纠正。

tips：**定点数的左右移位乘除 2 的方法并不适用于浮点数**运算。

e.g.浮点数加、减运算过程一般包括对阶、尾数运算、规格化、舍入和判断溢出等步骤。设浮点数的阶码和尾数均采用补码表示，且位数分别为 5 和 7 (均含 **2 位符号位**)。若有两个数， \\(X = 2^7×29/32\\) 和 \\(Y = 2^5×5/8\\) ，求浮点加法计算 \\(X+Y\\) 的最终结果。

*   写补码：X 阶码 7 的补码为 00,111；尾数 29/32 = 0.90625 = 00.11101。Y 阶码 5 的补码为 00,101；尾数 5/8 = 00.10100。
*   对阶：将 Y 的阶码调整为 7 尾数右移 2 位，得到 \\(Y' = 2^7×0.0010100\\) 。
*   尾数运算：相加结果为 01.0001000（双符号位 01 **发生正溢出**，需规格化）。

（4）C 语言的浮点类型与转换

C 程序中等式的赋值和判断会导致**强制转换**，char -> int -> long -> double 和 float -> double 都是范围和精度从小到大，**转换过程没有损失**。不同类型数的混合运算遵循**低类型转换为高类型**，如 float 型和 double 型运算结果为 double 型，这种转换称为**隐式转换**。

![](https://pica.zhimg.com/v2-f6d00610829ae7830a3387b0808f167e_r.jpg)

e.g.假设已定义三个 int 型变量 x、y、z，sizeof(int)=4，double 型采用 IEEE754 双精度浮点数格式：

*   double dx=(double)x；double dy=(double)y；double dz=(double)z；
*   double 型可以精确地表示 int 型，且对阶时尾数移动位数不会超过 52 位，因此尾数不会舍入，于是不会发生大数吃小数的情况；
*   x+y 可能溢出，而 dx+dy 不会溢出；double 型不管结果是否溢出，都**不会影响乘积的符号**。

三、计算机存储系统
---------

### 1、存储器基础

（1）存储器的分类

按作用区别可以分为主存、辅存和高速缓存 Cache：

![](https://pic4.zhimg.com/v2-020648fce4264c5752b414b270190279_r.jpg)

三级存储系统的层次结构和组成

*   Cache-主存层主要解决 **CPU 和主存速度不匹配**的问题，之间的数据调动是由**硬件自动完成**的，**对所有程序员均是透明**不可见的。
*   主存-辅存层主要解决**存储系统的容量**问题，之间的数据调动是由**硬件和操作系统共同完成**的，采用虚拟存储技术，**对应用程序员是透明**不可见的，可以在更大的虚拟地址空间编程。

按存取方式可以分为以下类别：

![](https://pica.zhimg.com/v2-d1b6d212d90b5a752e71f2f51527305a_r.jpg)

此外还有**串行**访问存储器，分为**顺序存取 SAM**（如磁带）和**直接存取 DAM**（如磁盘光盘）。

（2）存储器的性能指标

存储器追求大容量、低成本和高速度，三个主要性能指标分别是：

**存储容量** = 存储字数×字长。存储字数表示存储器的**可寻址单元数**，字长表示**一次存取操作的数据量**。

*   若机器字长为 64 位，CPU 按字寻址即一次存取操作 8B 即 64 位数据量。
*   存储器容量为 1024×8 位，即需要 8 根数据线，10 根地址线，寻址空间为 1024=1K。

**单位成本**：位价 = 总成本/总容量。

**存取速度**：数据传输速率（每秒传送信息的位数）= **数据的宽度/存取周期**。

*   存取时间 T\_a 是指从**启动一次存储器读出或写入**所需的平均时间。
*   存取周期 T\_m 是指**存储器连续读写**所需的**最短间隔**，通常大于存取时间。
*   主存带宽 B\_m 也称**数据传输速率**，表示每秒从主存进出信息的最大数量。

![](https://pic3.zhimg.com/v2-318eaf90113fa287d65315d592e0a1ea_r.jpg)

任何一种存储器在读/写操作之后，总要有一段恢复内部状态的复原时间

![](https://pic4.zhimg.com/v2-0bfb7c9ad1b940450914fea132982deb_r.jpg)

计算机系统通常采用的多级存储结构，解决容量、速度、价格之间的矛盾

### 2、主存储器

（1）主存的基本组成

主存由 RAM 和 ROM 构成，两者**统一编址**，都支持**随机存取访问信息**，即访问时间与存储器物理位置无关。

![](https://pic2.zhimg.com/v2-f4ce9d9d3c70dd461ddc7336a92c664f_r.jpg)

主存储器的基本组成框图

存储器最基本构件是存放一个二进制位的存储元，**地址码相同的存储元构成存储单元**，若干**相同宽度的存储单元**构成的存储阵列/存储体是存储器的核心部件，为了存取存储体中的信息，必须对存储单元编址。编址单位是指具有相同地址的那些存储元件构成的一个单位，现代计算机通常采用字节编址方式，**一个地址对应 1 字节**。

指令执行过程中需要访问主存时，CPU 首先把**被访问单元的地址送到 MAR** 中，然后**通过地址线将主存地址送到主存中的地址寄存器**，以便地址译码器进行译码，选中相应单元，同时 CPU 将**读/写控制信号通过控制线送到主存的读/写控制电路**。

*   写操作：CPU 同时将要写的信息送到 MDR 中，在读写控制电路的控制下经数据线写入选中单元。
*   读操作：主存读出选中单元的内容送至数据线，然后被送到 MDR 中。

tips：**MDR 的位数与数据线的位数相同，MAR 的位数与地址线的位数相同**。

*   采用 64 位**数据线**，所以在按字节编址方式下，每次最多可以存取 8 个单元的内容。
*   采用 36 位**地址线**，最大寻址范围为 0～ 2^{36} - 1，即地址从 0 开始编号。

（2）随机存取存储器 RAM（Random Access Memory）

RAM 是计算机中用于临时存储数据和程序的主要内存设备，读写速度快，但是**断电后数据会丢失**，因此属于**易失性存储器**。

RAM 主要分为静态 SRAM 和动态 DRAM 两种类型：

*   SRAM 使用**触发器**来存储数据，每个存储元由**双稳态** **6 个 MOS 管**分别存储 01，速度快且不用周期性刷新电路，但成本高功耗大，集成度较低，常用于**高速缓存**如 Cache、TLB 等。
*   DRAM 使用**电容**来存储数据，每个存储元由 **1 个 MOS 管加电容**组成，容量更大且集成度高，成本低但由于电容漏电会需要周期性频繁重写数据，常用于**主存储器**如内存条等。

DRAM 电容上的电荷一般只能维持 1～2ms，刷新可以采用读出的方法进行即**读后再生**。对**同一行进行相邻两次刷新的时间间隔称为刷新周期**，通常取 2ms。刷新**不依赖于外部也不选片**，常用方法有：

*   **集中刷新**：在一个刷新周期内依次对存储器的所有行进行逐一再生，在此期间停止对存储器的读/写操作，称为访存死区。优点是**读/写操作期间不受刷新操作的影响**；缺点是**死区不能访问存储器**。
*   **分散刷新**：将一个存储器芯片的工作周期分为前半用于正常的读/写操作；后半用于刷新。优点是**没有死区**；缺点是**加长了系统的存取周期**。
*   **异步刷新**：在一个刷新周期内每一行仅刷新一次，即将刷新周期除以行数，得到相邻两行之间刷新的时间间隔，每隔时间 t 产生一次刷新请求。这样就使死区更加分散，**避免让 CPU 连续等待过长的时间**。

![](https://pic4.zhimg.com/v2-8527d1c2f55cc8ef1cb0c588423d9d67_r.jpg)

16K 寻址空间的 DRAM 有 128 行 128 列，一次刷新占一个存取周期，仅分散刷新不存在死区

e.g.采用 64K×1 位的 DRAM 芯片构成 128K×8 位的存储器，若采用异步刷新方式，每单元刷新间隔不超过 2ms，求生成的刷新信号的间隔时间和集中刷新方式存储器刷新一遍最少用的读写周期数。

64K×1 位的 DRAM 芯片由一个 256×256 的位平面组成。构成存储器的所有芯片**同时按行刷新**，每个芯片有 256 行，所以存储器**所有单元刷新一遍至少需要 256 次刷新操作**。

*   若采用异步刷新方式，则相邻两次刷新信息的时间间隔为 2ms/256≈7.8μs 。
*   若采用集中刷新方式，则刷新一遍最少需 256 个读/写周期，该过程中存储器不能读/写。

tips：DRAM **刷新是以行为单位逐行恢复数据**，**再生仅恢复被读出的单元数据**；采用地址引脚复用技术即**行地址和列地址通过相同的引脚先后两次输入**来减少地址引脚数，因此**行列位数尽量接近**，且由于按行刷新，应使得**行数较少**；每增加一根地址线，容量至少提高到原来四倍。

![](https://pica.zhimg.com/v2-bd568632d15c5a4173c2ba4a2ed58a26_r.jpg)

DRAM 芯片内部结构，存储单元数=位平面数×行数×列数

DRAM 芯片容量为 16×8 位需 2 根地址线 8 根数据线，前者**分时传送 2 位行地址和 2 位列地址**。芯片内部有一个 **SRAM 实现的行缓冲器**，用来缓存指定行中每列的数据，其大小为**列数×存储元的位数**，可支持突发传输（指在寻址阶段给出数据的首地址，在**传输阶段可传送多个连续存储单元**的数据）。

*   目前更常用的是 SDRAM 芯片，它与 CPU 的数据交换**同步于系统的时钟信号**，将 CPU 发出的地址和控制信号锁存起来，经过指定的时钟周期数后再响应，此时 CPU 可执行其他操作，并且**以 CPU-主存总线的最高速度运行，而不需要插入等待状态**。
*   而传统 DRAM 中，CPU 将地址和控制信号送至存储器后，**需经过一段延迟时间，数据才读出或写入**，且 CPU 在读写完成之前不能做其他工作，降低了 CPU 的执行速度。

现代存储器 **DDR** 即双倍数据率（Double Data Rate）SDRAM，通过在时钟信号的上升和下降沿均传输数据，从而在**每个时钟周期内能够传输两次数据**，实现其**传输速率是其时钟频率的两倍**。

![](https://picx.zhimg.com/v2-fff4a2a413234cf59be44a0925f636ef_r.jpg)

（3）只读存储器 ROM（Read Only Memory）

ROM 结构简单位密度高，其中存储的数据一旦被写入，就不能轻易修改或删除，具有**不易擦写**、**永久存储**、**快速访问**和**随机存取**的特点。主要分为以下几种类型：

**掩模只读存储器** Mask ROM：在制造过程中使用特殊的掩模技术将数据直接写入芯片中，一旦数据写入，就无法更改，成本较低，适合大批量生产。

**一次编程只读存储器** PROM：用户可以编程一次将数据写入，但之后就无法再修改，适合小批量。

**可擦除可编程只读存储器** EPROM：通过紫外线等多次读写使用并改写，缺点是整个芯片如只写错一位也必须从电路板上取下擦掉重写，擦写次数有限，**不能作为随机存储器**。

**电可擦可编程只读存储器** EEPROM：用户可以通过电子擦除和编程来修改存储的数据，擦写速度相对较慢，但擦写次数大大提高，且可以单独擦除和编程。**常存放 PC 机中 BIOS 引导程序和 MAC 地址**。

tips：CD-ROM 是只读光盘存储器，Flash ROM 是闪存只读存储器（常用作 U 盘、SD 卡和 SSD 硬盘），两者都是**外存**，不属于上述内存的 ROM 概念。

（4）多模块存储器

多模块存储器是一种**空间并行**技术，利用多个结构相同的存储模块并行工作来体感存储器吞吐率。

**单体多字存储器**：每个存储单元存储 m 个字，总线宽度也为 m 个字，一次并行读出 m 个字。在一个存取周期内，从同一地址取出 m 条指令逐条送至 CPU 执行，即每隔 1/m 存取周期，CPU 向主存取一条指令。

*   优点：大幅提高了单体存储器的工作速度，**解决访存速度问题**。
*   缺点：只有**指令和数据在主存中连续存放**时，这种方法才能有效提升存取速度。

**多体并行存储器**：由多体模块组成，每个模块都有**相同的容量**和**存取速度**和**独立的读/写控制电路**、地址寄存器和数据寄存器，既能并行工作，又能交叉工作。分为高位交叉编址和低位交叉编址两种。

*   高位交叉编址（顺序方式）：**高 2 位地址为模块号**，低位地址为模块内地址。访问一个连续主存块时，CPU 总是先在一个模块内访问完才转到下一个模块，**各模块不能被并行访问**。
*   低位交叉编址（交叉方式）：**低 2 位地址为模块号**，高位地址为模块内地址。**程序连续存放在相邻模块中但各单元地址不连续**，可以采用轮流启动（模块数×总线周期 \\geq 存取周期）或同时启动两种方式。

![](https://pic3.zhimg.com/v2-cd0b8ff84866a7aa3a20095b216f0bb4_r.jpg)

高位交叉与低位交叉编址的多体存储器，模块内地址连续，仍是串行存取，因此还是顺序存储器

e.g.用若干 16K×8 位的存储芯片组成一个 64K×8 位的存储器，芯片各单元采用**交叉编址**方式，求地址 BFFFH 所在的芯片的最小地址。

易知芯片数为4。芯片各单元采用交叉编址方式，所以每个芯片的模块号由最低两位地址确定，分别为 00、01、10、11，高 14 位为片内地址。**地址 BFFFH 最低两位为 11**，故其所在芯片的最小地址为 0003H。

tips：低位交叉编址中**模块序号=访存地址%存储器交叉模块数**，若给定的访存地址在相邻 m 次访问中出现在同一个存储模块内，则判定可能冲突。一个存取周期内可连续访问 m 个模块。

轮流启动：**每个存储模块一次读写的位数正好等于数据总线位数**，模块存取周期为 T，总线周期为 r：

![](https://pica.zhimg.com/v2-6810fd33808ac4756e062ef21773f466_r.jpg)

低位交叉轮流启动存取 m 个字所需 T+(m-1)r 时间

同时启动​：**所有模块一次并行读/写的总位数正好等于数据总线位数**。充分利用总线带宽。

（5）主存储器与 CPU 的连接

*   数据总线的位数与工作频率的乘积正比于数据传输速率。
*   地址总线的位数决定了可寻址的最大内存空间。
*   控制总线（读/写）指出**总线周期的类型**和本次**输入/输出操作完成的时刻**。

![](https://pica.zhimg.com/v2-39c1991d53628cb3cadebbe9d9d6a102_r.jpg)

位扩展是对字长进行扩展，即**增加存储字长**，是实现多模块交叉编址的一种手段。

*   **应用场景**：当 CPU 的系统数据线数多于存储芯片的数据位数时，必须对存储芯片扩位，使其数据位数与 CPU 的数据线数相等。
*   **连接方式**：各芯片的地址线、片选控制线和读/写控制线与系统总线相应**并联**；数据线单独引出，分别连接系统数据线。各芯片**同时工作**。

![](https://pic1.zhimg.com/v2-25e2ddf70fd0ae66b9d408e1451e0a0c_r.jpg)

8 片 8K×1 位的 RAM 芯片组成的 8K×8 位存储器位扩展与 CPU 连接

字扩展是对**存储字的数量进行扩展**，即存储字的位数满足系统要求，而**改变地址线**。

*   **应用场景**：系统数据线位数等于芯片数据线位数，**系统地址位数多于芯片地址位数**。
*   **连接方式**：各芯片的**地址线与系统地址线的低位**对应相连；数据线和读/写控制线与系统总线相应并联；由系统地址线的**高位译码得到各芯片的片选信号**。各芯片**分时工作**。

![](https://pic2.zhimg.com/v2-8c15b9501d9563de13960120b2aadf41_r.jpg)

4 片 16K×8 位的 RAM 芯片组成的 64K×8 位存储器字扩展与 CPU 连接

*   A\_{15}A\_{14} 作为片选信号，为 00 时译码器输出端 0 有效，选中 1 号芯片；为 01 时译码器输出端 1 有效，选中 2 号芯片；为 10 时译码器输出端 2 有效，选中 3 号芯片；以此类推 4 号芯片。
*   第一片芯片，最低地址：**00**00000000000000000；最高地址：0011111111111111
*   第二片芯片，最低地址：**01**00000000000000000；最高地址：0111111111111111
*   第三片芯片，最低地址：**10**00000000000000000；最高地址：1011111111111111
*   第四片芯片，最低地址：**11**00000000000000000；最高地址：1111111111111111

字位同时扩展可以既增加存储字的容量又增加存储字长。它将**进行位扩展的芯片作为一组**，各组的连接方式与位扩展的相同；由**系统地址高位译码产生若干片选信号**，分别接到各组芯片的片选控制线。

![](https://pic3.zhimg.com/v2-6d1ee75048d88bca0c03af1332eb58ce_r.jpg)

8 片 16K×4 位的 RAM 芯片组成 64K×8 位的存储器字位同时扩展与 CPU 连接

e.g.用若干 2K×4 位的芯片组成一个8K×8位的存储器，求地址0B1FH所在芯片的最小地址。

由题知位扩展 8位 / 4位 = 2片，字扩展 8K / 2K=4片，共需 8 片，每两片 2K×4位芯片并联构成 2K×8位的芯片组，每组 2K 个地址。**存储器的地址是按芯片组分配的**，对应的地址范围为：

*   第0组：0000H∼07FFH；第1组：0800H∼0FFFH
*   第2组：1000H∼17FFH；第3组：1800H∼1FFFH

地址 0B1FH ​**​所在芯片即第1块​**​（0800H∼0FFFH），最小地址就是 0800H。

e.g.内存地址区间为 4000H 至 43FFH，每个存储单元可存储 16 位二进制数，该内存区域由 4 片存储器芯片构成，求构成该内存所用的存储器芯片的容量。

43FF - 4000 + 1 = 400H，即内存区域为 1K 个地址单元，总容量为 1K×16 位。现由 4 片存储芯片构成，则构成该内存的芯片容量为 1K×16 位/4 = 512 字节。

e.g.32K×16 的存储芯片有地址线 15 根（片内地址），片选地址为 3 位，因此地址总位数为 18 位，现高 3 位为 111，则首地址为 **111**000000000000000 = 38000H，末地址为 **111**111111111111111 = 3FFFFH。

存储芯片的地址分配和片选方法，包括线选法和译码片选法：

*   **线选法**：高位地址线直接连接到各个存储芯片的片选端，当某位地址线信息为“0”时，选中对应芯片。**不需要地址译码器**，线路简单，**但地址空间不连续**，**不能充分利用系统的存储器空间**。
*   **译码片选法**：使用译码器将高位地址线译码产生片选信号，**地址空间连续**，**能充分利用存储器空间**，适用于地址线多于片选信号的情况。

### 3、外存储器

辅助存储器（Secondary Storage）也常称为外存，用于长期地存储数据和程序，包括机械硬盘（HDD）、固态硬盘（SSD）、光盘（CD、DVD）、磁带（Magnetic Tape）和闪存驱动器等。其中**磁带只能顺序访问**对数据读写，**光盘只能串行存取**对数据读写，而**磁盘和 U 盘等可以随机访问按任意次序**对数据读写。

辅助存储器容量大，能够存储大量数据，且能随机访问，适合频繁访问的数据存储，但读写速度相对较慢。

（1）磁盘存储器

磁盘存储器是以磁盘 Disk 为存储介质的存储器。

*   **优点**：存储容量大，价格低；记录介质可重复使用；记录信息可长期保存而不丢失，甚至可脱机存档；非破坏性读出，读出时不需要再生。
*   **缺点**：存取速度慢，且机械结构复杂，对工作环境要求较高。

磁盘存储器由磁盘驱动器、磁盘控制器和盘片组成。

*   **磁盘驱动器**：驱动磁头和盘片移动并发出信号，在盘面上通过**磁头进行串行读写**操作。
*   **磁盘控制器**：磁盘驱动器与主机的接口，负责**接收并解释 CPU 发来的命令**，向磁盘驱动器**发出各种控制信号**，并负责检测磁盘驱动器的状态。

![](https://pic1.zhimg.com/v2-3e4c7fdede7110de70753b364963cefa_r.jpg)

一个磁盘含有若干记录面，每个记录面划分为若干圆形的磁道，而每条磁道又划分为若干扇区，**扇区是磁盘读写的最小单位**，**包含数据、地址和校验等信息**。文件实际**存放磁盘及其读写均以块为单位**。

*   **磁头数**：即记录面数，磁头用于读取盘片上记录面的信息和写入信息，一个记录面对应一个磁头。
*   **柱面数**：即每面磁道数。在一个盘组中，不同记录面的相同编号（指磁道号）的磁道构成一个柱面。
*   **扇区数**：即每条磁道上有多少个扇区。
*   磁盘的物理地址：**磁道号+盘面号+扇区号**可以唯一确定磁盘块的位置。

相邻磁道及相邻扇区间通过一定的间隙分隔开，以避免精度错误。扇区按固定圆心角度划分，因此**位密度从最外道向里道增加**，磁盘的**存储能力受限于最内道的最大记录密度**。

**坏块产生​**​：磁盘因有移动部件且容错能力弱，易导致扇区损坏，部分磁盘出厂时就存在坏块，处理方式因磁盘和控制器而异，处理实质是**避免系统使用它们**。

*   **简单磁盘坏块处理​**：以采用 IDE 控制器的磁盘为例，可通过 MS - DOS 的 Format 命令进行**逻辑格式化**来扫描并**标记坏块于 FAT 表**，防止程序使用。
*   **复杂磁盘坏块处理​​**：控制器维护一个在出厂**低级格式化**时初始化并**动态更新的坏块列表**，低级格式化会预留备用块，控制器通过“扇区备用”机制用备用块替换坏块。

磁盘的性能指标：

*   **记录密度**：盘片单位面积上的二进制数据量，通常以磁道密度、位密度和面密度表示。道密度是沿磁盘半径方向单位长度上的磁道数，位密度是单位长度上能记录的二进制位数，而面密度是前两者乘积。
*   **磁盘容量**：非格式化容量 = 记录面数×柱面数×**每条磁道的磁化单元数**；格式化容量 = 记录面数×柱面数×**每个扇区的容量**。格式化后的容量会 **减小**。
*   **存取时间**：由**寻道平均**时间（磁头移动到目的磁道）+ **旋转半周**时间 + **传输数据**时间三部分构成。
*   **数据传输速率**：磁盘存储器在单位时间内向主机传送数据的字节数。即**转速与磁道容量之积 rN**。

tips：转速翻倍，对**寻道时间无影响**，平均**旋转延迟时间减半**，存取速度提高，**传输速率翻倍**。

独立冗余磁盘阵列 RAID 是指将**多个独立的物理磁盘组成一个独立的逻辑盘**，数据在多个物理盘上分割**交叉存储**、**并行访问**，具有更好的存储性能、可靠性和安全性，可随时**热插拔**磁盘而不损坏数据。

*   RAID0：无冗余和无校验的磁盘阵列，**条带化读写速度快**，但**没有容错能力**。
*   RAID1：镜像磁盘阵列，使**两个磁盘同时进行读/写**，互为备份。
*   RAID2：采用纠错的海明码的磁盘阵列。
*   RAID3：位交叉奇偶校验的磁盘阵列。
*   RAID4：块交叉奇偶校验的磁盘阵列。
*   RAID5：**无独立校验**的奇偶校验磁盘阵列。

tips：RAID 不会影响磁记录密度也不会提高磁盘利用率。

（2）机械硬盘与固态硬盘

![](https://pic3.zhimg.com/v2-f3d91b99ff9138ce5b5d339e202a27e2_r.jpg)

**机械硬盘** HDD：使用**旋转磁盘和磁头**读取数据的磁盘存储器，**存储容量大、成本较低但读写速度极慢且可靠性低**，适用于需要大容量存储和相对较低成本的场景，逐渐被固态硬盘所取代。

**固态硬盘** SSD：使用**闪存芯片**存储数据。它采用了非易失性存储技术，如 NAND 闪存，**随机存取速度快、访问延迟低、能耗低、可靠性高但存储容量通常较小且价格较高**，不需要磁盘调度，适用于需要高速、可靠性和耐用性的场景，如高性能计算、游戏电脑等。

![](https://pica.zhimg.com/v2-a03cc79d4daeaf33a15fe20e8a0f2296_r.jpg)

闪存翻译层将来自 CPU 的逻辑块读写请求翻译成对底层物理设备的读写控制信号

一个闪存由 B 个块组成，每块由 P 页组成。通常页的大小是 512B～4KB，每块由 32～128 页组成，SSD 数据是**以页为单位读/写，以块为单位擦除**，只有在一页所属的块整个被擦除后，才能写这一页。一旦一个块被擦除，块中的每个页就都可以直接再写一次。某个块进行了若干次重复写之后，就会磨损坏，不能再使用。

SSD 磨损均衡技术大致分为两种：

*   **动态磨损均衡**：写入数据时，**优先选择擦除次数少**的新闪存块，通常**不如静态**磨损均衡。
*   **静态磨损均衡**：没有数据写入，SSD 也会监测并自动进行数据分配，让**老的闪存块承担以读为主**的存储任务。同时让较**新的闪存块承担更多以写为主**的存储任务。

tips：硬盘与内存交换数据时，由 **DMA 控制器**操作控制。

### **4、高速缓冲存储器**

（1）程序访问的局部性原理与 Cache 认识

程序局部性是指在一段时间内，整个程序的执行仅限于一个较小的局部范围内。具体表现为以下两种：

*   **时间局部性**：如果一个数据被访问了，那么它在之后也很可能再次被访问。例如程序中的**循环结构和调用**过程。
*   **空间局部性**：如果一个数据被访问了，那么它内存中相邻数据也很可能很快被访问。例如程序中的**数组、结构体成员、顺序执行的代码块以及指令流**通常顺序存放。

计算机组成原理考研笔记六万字整理(3)
-------------------

Cache 位于 CPU 和主存之间作为一个缓冲，用于**复制频繁访问的数据和指令**，让 CPU 在需要时可以快速地访问缓存中的数据，**减少 CPU 访问主存的次数**来提高数据访问速度。Cache 功能**全部由硬件实现**。

tips：CPU 访存时先到 Cache 中查看有无主存地址记录，发送的是**主存物理地址**。

Cache **由 SRAM 组成**，采用**指令缓存 I-Cache** 和**数据缓存 D-Cache** 分离形式，让取指和取数在不同 Cache 寻找，**减少指令流水线的冲突**，常有多级缓存：

*   L1 Cache 位于 CPU 内部，速度最快但容量最小，用于**存储最频繁访问的数据和指令**。
*   L2 Cache 通常也集成在 CPU 芯片上，容量较大但速度略慢，用于扩展并**提高缓存命中率**。
*   L3 Cache 通常位于 CPU 芯片外部，但仍然与 CPU 连接紧密，容量最大但速度相对较慢，用于为多个 CPU 核心**提供共享的高速缓存**，以**减少核心之间的竞争**，提高系统整体性能。

（2）Cache 的工作原理

为便于 Cache 与主存交换信息，**Cache 和主存都被划分为大小相等的块**，Cache 块由若干字节组成，块数要远少于主存中的块数，即 Cache 中**仅保存主存中最活跃的若干块的副本**。

![](https://pica.zhimg.com/v2-1c5a61c9274fdb43139aac0caa22d6ee_r.jpg)

当 CPU 发出**读请求**时访存地址：

*   若 Cache 命中，就将此地址转换成 Cache 地址，直接对 Cache 进行读操作，与主存无关。
*   若 Cache 不命中，则仍需访问主存，并把此字所在的块一次性地从主存调入 Cache；若此时 Cache 已满，则需用替换算法将这个块替换 Cache 中原来的某块信息。

**CPU 与 Cache/主存**之间的数据交换**以字为单位**，而 **Cache 与主存**之间的数据交换则**以 Cache 块为单位**。

当 CPU 发出**写请求**时，若 Cache 命中，有可能遇到 Cache 与主存中的内容不一致的问题，则需要按照一定的写策略处理，写命中处理方法有以下两种：

*   **直写法**：必须**同时将数据写入 Cache 和主存**。实现**简单安全**但增加了访问次数，降低了 Cache 效率，可能导致写缓冲饱和和溢出。**L1 Cache 对 L2 Cache 使用**。
*   **回写法**：数据先写入 Cache **不立即写入主存**，只有在**被替换出 Cache 时才写回主存**。减少访问次数**提高效率**，但存在数据不一致的隐患。**L2 Cache 对主存使用**。

tips：Cache-主存层次可采用回写法或直写法，但**主存-外存层次一般只用回写法**。写策略决定 Cache 是否需要修改位，直写法不需要，**回写法要每个 Cache 行需1位修改位**。

对于 Cache 写操作不命中也有两种处理方法：

*   **写分配法**：更新主存单元，然后**将主存块调入 Cache**，利用程序的空间局部性。
*   **非写分配法**：只更新主存单元，**不将主存块调入 Cache**。

**Cache 命中率**指 CPU 要访问的信息已在 Cache 的比率，是 Cache 性能重要因素。设 Cache 总命中次数为 N\_c ，访问主存总次数为 N\_m ，则命中率 H=\\frac{N\_c}{N\_c+N\_m} 。**主存块适中，Cache 容量越大命中率越高**。

1-H 称为缺失率，设 t\_c 为命中时的 Cache 访问时间， t\_m 为缺失时的访问时间，则 Cache-主存系统的平均访问时间 T\_a=Ht\_c+(1-H)t\_m ，**访存效率** e=\\frac{t\_c}{T\_a} 。

e.g.数据 Cache 每行 64B，数组 int a\[256\]\[256\] 在程序 A 和 B 分别按行优先逐行、列优先逐列访问：

*   Cache 缺失是按“**Cache 行**”大小为单位填充的。
*   **行优先局部性好**：数组按行存储时，同一行元素在主存中连续，逐行访问能利用空间局部性，可一次性加载 16 个元素到 Cache 行，每行均**第 1 个元素不命中，后续 15 个元素都命中**，命中率约为 93.75%。
*   **列优先局部性差**：数组按列访问时，列元素在主存中不连续（相隔一行的大小），无法利用空间局部性，相邻列的元素在主存中相隔 1024 B 远大于 Cache 行大小，**每次访问都要重新加载**，命中率接近 0。

根据 Cache 的读、写流程，可知实现 Cache 时需解决以下关键问题：

*   **数据查找**：如何快速判断数据是否在 Cache 中。
*   **地址映射**：主存块如何存放在 Cache 中，如何将主存地址转换为 Cache 地址。
*   **替换策略**：Cache 满后，使用何种策略对 Cache 块进行替换或淘汰。
*   **写入策略**：如何既保证主存块和 Cache 块的数据一致性，又尽量提升效率。

（3）Cache 和主存的映射方式

主存被划分的等长数据块，**Cache 行数据是主存中某个块的副本**，大小与 Cache 行一致。其有**标记位**和**有效位**分别指明是主存哪一块的副本，以及说明自己信息是否有效。

地址映射是指把**主存地址空间映射到 Cache 地址空间**，方法有以下 3 种：

![](https://pic2.zhimg.com/v2-1dfe973f278ec0a9bd7cbe9b6a194561_r.jpg)

主存与 Cache 数据块大小相同，主存块号即 Tag + 组号/行号，它们的块内地址相同

*   块内地址位数由**块大小/行大小**决定，如主存块大小为 64B，则块内地址占 6 位。
*   直接映射中 Cache **行号位数为 log₂ 行数**​​，组相联映射中 Cache **组号位数为 log₂ 组数**。
*   主存块标记 Tag 区分不同主存块映射到同一 Cache 行的情况，位数 **log₂(主存大小/Cache大小)**。

**直接映射**：主存中的每一块只能装入 Cache 中的**唯一位置**，若满则将原来的块直接替换出去。只需**一个比较器**，方法简单但**块冲突概率最高**，**空间利用率最低**，大容量数据 Cache 常用。

在直接映射方式中 Cache 行号 = 主存块号 % Cache 总行数，设 Cache 共有 2^c 行，即 Cache 行号 = 主存块号二进制数的后 c 位。设主存有 2^m 块：

*   主存的第 0 块、第 2^c 块、第 2^{c+1} 块……只能映射到 Cache 的第 0 行；
*   主存的第 1 块、第 2^c+1 块、第 2^{c+1}+1 块……只能映射到 Cache 的第 1 行，以此类推。
*   在直接映射中**行号和块内地址位数在主存与 Cache 中一致**，因此主存地址多出的长度即**主存 Tag 位数**。

**全相联映射**：主存中的每一块可以装入 Cache 中的**任何位置**，每个 Cache 行都设置**比较器成本高速度慢**。优点是**冲突概率低**，**利用率和命中率高**，用于小容量 TLB / 指令 Cache。

首先将主存地址的高位标记（位数 = log₂ 主存块数）与 Cache 各行的标记进行比较：

*   若有一个相等且对应有效位为 1，则命中，此时根据块内地址从该 Cache 行中取出信息。
*   若都不相等，则不命中，此时 CPU 从主存中读出该地址所在的一块信息送到 Cache 的任意一个空闲行中，将有效位置 1，并设置标记，同时将该地址中的内容送 CPU。
*   **主存-外存层次通常只使用全相联映射**方式。

**组相联映射**：将 Cache 分成 Q 个大小相等的组，每个主存块可以装入固定组中的任意一行。**组间采用直接映射，组内采用全相联映射**，当 Q=1 时为全相联映射，当 Q=Cache 行数时变为直接映射。

*   组号 = 主存块号 % Cache 组数；块内地址即一个主存块中有多少个编址单元。
*   **每个组包含 r 块 Cache 行**，常见的有 4 路、8 路、16 路等，**需要 r 个比较器**。
*   Cache 总容量 = Q \* r \* 行大小，**比较器位数同 Tag 位数**。

CPU 根据地址找到对应的 Cache 组，比较标记，若**标记相等且有效位为 1 则命中**，从 Cache 行中取出信息；若不命中，从主存中读出信息，送入 Cache 组的空闲行，设置标记和有效位。

e.g.某存储系统中，主存容量是 Cache 容量的 4096 倍，Cache 被分为 64 个块，当主存地址和 Cache 地址采用**直接映射**方式时，求地址映射表的大小。

*   **地址映射表即标记阵列**，为 Cache 的每一行存储对应的**主存标记信息以及有效位等控制信息**。
*   Cache 被分为 64 个块即有 64 行，因此标记阵列也有 64 行。
*   主存容量是 Cache 容量的 4096 倍即地址长度比 Cache 长 12 位，**主存 Tag 12 位**。
*   最后加上 **1 位有效位**，因此地址映射表的大小为 64×13 位。

e.g.有效容量为 128KB 的 Cache，每块 16B，即有 Cache 行 8K 个，采用 **8 路组相联**。字节地址为 1234567H 的单元调入该 Cache。

*   Cache 块大小为 16B 即**块内地址字段为 4 位**；
*   Cache 容量为 128KB，采用 8 路组相联，共有 8K/8 = 1024 组，**组号字段为 10 位**；
*   剩下的为标记字段。1234567H 转换为二进制数，**标记字段对应高 14 位**，即 048DH。

e.g.存储器按字节编址，主存容量为 1MB，Cache 容量为 16KB，每块有 8 个字，每字 32 位，采用**直接映射**方式，Cache 起始字块为第 0 块。已知主存地址为 35301H 且 CPU 访问 Cache 命中，要求选出该地址在 Cache 的第几个（十进制）字块中。

*   给定主存地址的二进制数为 0011 0101 0011 0000 0001。
*   因为每个 Cache 行大小 32B，**块内地址占低 5 位**。
*   因为 1MB/16KB = 64，主存**标记字段占高 6 位**。
*   剩余的 010011000 被标识为 **Cache 行号**，将其转换为十进制数是 152。

（4）Cache 中主存块的替换算法（**全相联、组相联映射需要考虑**）

当 Cache 行/组中的空间已满时，需要**硬件使用替换算法**决定哪个主存块被替换掉。

**LRU 算法**：基于局部性原理，选择最近**最久未访问过的 Cache 行进行替换**，命中率高。

*   为每个 Cache 行设置一个计数器（LRU 替换位），记录主存块的使用情况以选择淘汰块。
*   **组相联**映射：计数值位数与 Cache 组大小有关，替换位数即 log\_2路数 。
*   **全相联**映射：计数值位数与 Cache 行数有关，替换位数即 log\_2行数 。
*   命中时计数器清零，**比其低的计数器加 1**，其余不变；
*   未命中且有空闲行时，**新装入的行计数器置 0**，其余非空闲行全加 1；
*   未命中且无空闲行时，计数值为 3 的行的信息块被替换，新装入的行计数器置 0，其余全加 1。
*   当集中访问的**存储区超过 Cache 组大小**时，命中率可能极低，这种现象叫**抖动**。

以下两个均未依赖程序访问的局部性原理，**​不需要​**​**替换控制位**。

*   **RAND 算法**：随机确定替换的 Cache 行，实现**简单命中率低**。
*   **FIFO 算法**：对每个 Cache 行打一个**时间戳记录何时装入新主存块**，选择最早调入的 Cache 行进行替换。

e.g.某虚拟存储器系统采用页式内存管理，使用 LRU 页面替换算法 3 个页面初始为空，考虑下面的页面访问地址流：1 8 1 7 8 2 7 2 1 8 3 8 2 1 3 1 7 1 3 7 求未命中率。

按地址流**依次访问页面**，记录每次访问是否命中失效：

![](https://pic1.zhimg.com/v2-a06a4043bc9df3ce450f1a0bb3dcbba4_r.jpg)

内存中无该页面则加载，内存已满则更新，所以失效 6 次，未命中率 30%

![](https://pic3.zhimg.com/v2-d0b8b4dc8e71b74571e4f4c14cf2db42_r.jpg)

有效位和 tag 位必须设置，仅回写法需要1位脏位、组/全相联需要替换位

e.g.主存地址 32 位按字节编址，主存和 Cache 之间采用**全相联映射**方式，主存块大小为1个字，每字 32 位，采用**回写法**方式和**随机替换**策略，则能存放 32K 字数据的 Cache 的总容量至少有多少位。

*   主存块大小为1个字即 32 位，按字节编址，所以**块内地址占 2 位**。
*   在全相联映射方式下，主存地址只有两个字段，所以**标志占 30 位**。
*   Cache 总容量能存放 32K 字数据，**Cache 行同主存块大小位 1 字，即有 32K 行**。
*   因为采用回写法和随机替换策略，所以需 **1 位修改位**，**无须替换控制位**。

每个 Cache 行大小为 32bit（数据位）+ 30bit（tag位）+ 1bit（修改位）+ 1bit（有效位）= 64bit。综上， Cache 总容量至少应有 32K×64bit = 2048K bit。

e.g.主存地址为 32 位，按字节编址，指令 Cache 和数据 Cache 与主存之间均采用 **8 路组相联映射**方式，**直写法**策略和 **LRU 替换**算法，主存块大小为 64B，数据区容量各为 32KB。开始时 Cache 均为空，求每一行中标记、LRU 位各占几位？是否有修改位？

![](https://pic3.zhimg.com/v2-1380b21a431ea3c7b6b0cf3dda781fd4_r.jpg)

（5）缓冲区与缓存技术

缓冲区是一个存储区域，可以由**硬件寄存器**组成（成本高，容量小，速度块），也可以利用**内存作为缓冲区**，一般情况下更多是用后者，**设备独立性软件层**的缓冲区管理就是要组织管理好这些缓冲区。

*   T：设备把一块数据送进缓冲区所需时间；
*   M：数据从缓冲区拷贝到工作区的时间；
*   C：CPU 真正处理这块数据的时间。

**单缓冲**：操作系统为每个 I/O 请求分配一个缓冲区，必须**等缓冲区装满后才能取出数据**，若两个互相通信的机器设置单缓冲区，则同一时刻只能实现**单向数据传输**。

![](https://pic3.zhimg.com/v2-8b5c9f388122c331b79f0ca754c69bc0_r.jpg)

T 和 C 可以并行，处理一块数据平均耗时 Max(C, T)+M

**双缓冲**：操作系统为其分配两个缓冲区，若两个互相通信的机器设置双缓冲区，则同一时刻可以实现**双向的数据传输**。C 和 M 可以与 T 并行，处理一块数据平均耗时为 Max(T, C+M)。

![](https://pic4.zhimg.com/v2-786cebee3c8ebb84a3840ceab985bb5d_r.jpg)

![](https://pica.zhimg.com/v2-53a436f1064676ff84a04304089a7ce0_r.jpg)

e.g.某文件占 10 个磁盘块，现要把这些磁盘块逐个读入内存缓冲区并送到用户区分析，假设一个缓冲区与一个磁盘块大小相同，把一个磁盘块读入缓冲区的时间为 100μs，将缓冲区的数据传送到用户区的时间是 50μs，CPU 分析一块数据的时间为 50μs。则：

*   单缓冲结构要**等上一个磁盘块从缓冲区读入用户区完成才能读下一块**：最后一块磁盘块离开用户区用时 150×10=1500μs，加上 CPU 处理最后一个磁盘块 50μs 故总用时 1550μs。
*   双缓冲结构**不需要等待磁盘块从缓冲区读入用户区**：10×100+50+50=1100μs。

**循环缓冲**：将多个大小相等的缓冲区**链接成一个循环队列**，每个缓冲区有指向下一个缓冲区的指针，指针沿链接方向循环移动，最后一个缓冲区指针指向第一个缓冲区。

![](https://pic3.zhimg.com/v2-780a1f7fd0123c0d17e6293cd67a738a_r.jpg)

图示缓冲区以橙色表示数据满，绿色表示空缓冲区

**缓冲池**：位于主存，包含多个系统公用的缓冲区，由管理结构和操作函数组成，可供多个进程共享。

*   缓冲区分类：空缓冲队列、装满输入数据的输入队列、装满输出数据的输出队列。
*   四种工作缓冲区：hin 收容输入数据；sin 提取输入数据；hout 收容输出数据；sout 提取输出数据。

![](https://pic4.zhimg.com/v2-22b8c3c27fb9b787d8f4aca1a42b9e89_r.jpg)

*   收容输入：数据输入工作缓冲区后挂到输入队列队尾。
*   提取输入：计算进程从输入队列取缓冲区提取数据后挂到空队列队尾。
*   收容输出：计算进程取空缓冲区装满后挂到输出队列队尾。
*   提取输出：输出进程从输出队列取缓冲区提取后挂到空队列队尾。  
    

![](https://pic4.zhimg.com/v2-4860e7c9ebd220a61de3c219a2bb7dcb_r.jpg)

虚拟存储系统部分详见操作系统文章~

四、计算机指令系统
---------

### 1、指令系统认识

（1）指令集体系结构 ISA

![](https://pic4.zhimg.com/v2-0c303d78a53b1f1d593a1e96aeeba5fd_r.jpg)

计算机系统各层关系

机器指令是指示计算机执行某种操作的命令。一台计算机的所有指令的集合构成该机的指令系统，也称指令集，是指令集体系结构 ISA 的核心，**直接影响计算机性能和成本**。软硬件设计成员根据指令系统分别设计计算机的系统软件与硬件逻辑电路。

ISA **完整定义了软件和硬件之间的接口，规定了机器级程序的格式**。规定的内容主要包括：

*   指令格式，指令寻址方式，以及每种操作对应的操作数的相应规定。
*   操作数的类型，操作数寻址方式，以及是按大端方式还是小端方式存放。
*   程序访问的寄存器编号，个数和位数，存储空间的大小和编址方式。
*   指令执行过程的控制方式等，包括程序计数器、条件码定义方式。

（2）指令基础

一条指令就是**机器语言的一个语句**即一组二进制代码，通常包括**操作码字段和地址码字段**两部分。

*   操作码指出**该指令应执行什么操作具有何种功能**，如算术加减运算、是程序转移还是返回操作，操作码位数决定了指令数量。
*   地址码给出**操作数的地址**，包括参加运算的一个或多个操作数的地址、运算结果的保存地址、程序的转移地址、被调用子程序的入口地址等。

![](https://picx.zhimg.com/v2-84b1e535e1e9080747a569885b621191_r.jpg)

CPU 执行指令的过程中，指令的地址由程序计数器 PC 给出，操作数的地址由地址码给出

指令字长是指一条指令所包含的二进制位数，取决于操作码和地址码的长度，**与机器字长没有固定的关系**。指令字长越长，占用主存空间越大，耗费的访存次数越多。

*   **单字长指令**：指令长度等于存储字长的指令，只需**访存 1 次就能将指令完整取出**。
*   **半字长指令**：指令长度等于半个存储字长的指令，**访存 1 次可以读取两条指令**。
*   **双字长指令**：指令长度等于两个存储字长的指令，**访存 2 次才能完整取出**，耗费 2 个存取周期。
*   **定长**指令系统：所有指令的长度都相等，指令字**最高位部分分配固定的若干位表示操作码**，结构简单有利于指令顺序寻址取址和译码，但扩展性差。
*   **变长**指令系统：所有指令的长度都可变，指令字的操作码字段**位数不定且分散不同位置**上，结构灵活较难实现，可扩展性好且冗余少。

tips：现代主存普遍按**字节编址**，存储字长是**主存一次读写的字节数**；指令字长通常设计为**存储字长的整数倍**（或分数倍），以优化取指的访存效率。

根据指令中操作数**地址码数目的不同**，可将指令分成以下几种格式：

*   **零地址指令**：只给出操作码 OP，没有显式地址，也称堆栈运算指令，参与的两个操作数来自**栈顶和次栈顶**单元，用于**空操作**指令、**停机**指令、**关中断**指令等。
*   **一地址指令**：包含操作码 OP 和地址 A1，操作数按地址 A1 读取操作数，执行 OP 操作，另一个操作数通常存放在 ACC 中属于**隐含寻址**，结果存回 A1。
*   **二地址指令**：包含操作码 OP 和两个地址 A1、A2，用于**算术和逻辑运算**指令，操作数来自 A1 和 A2，结果存入A1。
*   **三地址指令**：包含操作码 OP 和三个地址 A1、A2、A3，用于**子程序控制**指令，操作数来自 A1 和 A2，结果存入 A3。
*   **四地址指令**：包含操作码 OP 和四个地址 A1~A4，用于**转移**指令，操作数来自 A1 和 A2，结果存入 A3，A4 为下一条指令地址。

同样指令的操作码也可以分成以下两类：

*   定长操作码：长度及其在指令中的位置均**固定**，译码和硬件实现简单，指令系统的指令数量 x 与操作码位数 n 满足 log\_2 ​ x≤n 。
*   变长操作码：长度及其在指令中的位置均**可变**，增加指令的数量，常用扩展操作码实现（**操作码长度随地址码的减少而增加**）。

![](https://pic3.zhimg.com/v2-7f14b60c48de0658e1947b862eefa904_r.jpg)

在 16 位定长指令系统通过操作码扩展来支持不同地址数的指令，同时充分利用指令编码空间

![](https://picx.zhimg.com/v2-5f5dca58fe4dbfd3df1c704c9311242b_r.jpg)

e.g.计算机系统采用 32 位单字长指令，地址码为 12 位，若定义了 250 条二地址指令，求可以有几条单地址指令。二地址指令包含**两个地址码**因此操作码长度 = 32 - 12 - 12 = 8 位。

已定义 250 条二地址指令，因此**剩余的编码数量**为：256 - 250 = 6 个。即可以设计出单地址指令 \\(6×2^{12} = 3×2^{13}\\) 条。

指令执行时，CPU 从主存中读出指令，通过译码器解释指令，执行操作。操作数直接寻址范围为 2^{地址码位数} ，若地址码字段均为主存地址，则完成一条指令需要**取指令一次，再取操作数，最后存结果一次**。

（3）指令的操作类型

计算机指令系统中按功能划分的五种操作类型，具体如下：

*   ​**​数据传送​**​：涵盖寄存器间、内存与 CPU 寄存器间、CPU 寄存器与内存间以及栈操作的数据传送指令，如MOV、LOAD、STORE、PUSH、POP 等。
*   ​**​算术和逻辑运算​**​：包含加、减、乘、除、增 1、减 1 以及逻辑运算等指令，如 ADD、SUB、MUL、DIV、INC、DEC、AND、OR、NOT、XOR 等。
*   ​**​移位操作​**​：主要有算术移位、逻辑移位、循环移位等指令。
*   ​**​转移操作​**​：包括无条件转移 JMP、条件转移 BRANCH、调用 CALL、返回 RET、陷阱 TRAP 等指令。
*   ​**​输入输出操作​**​：用于实现 CPU 与外部设备间的数据交换、控制命令传送及状态信息传递。

tips：**程序控制类**指令用于**改变程序执行的顺序**，包括**无条件转移、有条件转移、子程序调用、返回指令和循环指令**等。无条件转移指令在任何情况下都执行转移操作，而有条件转移指令仅在特定条件满足时才执行转移操作，转移条件一般是某个标志位的值，或几个标志位的组合。

调用指令和转移指令的区别：执行调用指令时必须**保存下一条指令的地址**（返回地址），当程序执行结束时，根据返回地址**返回到主程序继续执行**；而转移指令则**不返回执行**。

运算型指令寻址的是**操作数**，转移型指令寻址的是**下次将要执行的指令地址**。

### 2、指令的寻址方式

寻址方式是指**寻找指令或操作数有效地址**的方式，即确定本条指令的数据地址及下一条待执行指令的地址的方法。寻址方式分为指令寻址和数据寻址两大类。

寻找**下一条将要执行的指令**地址称为**指令寻址**​​；寻找**本条指令的数据**地址称为**数据寻址**。采用不同寻址方式是为了**缩短指令字长、扩大寻址空间、提高编程灵活性、但也提高了译码复杂度**。

（1）指令寻址

指令寻址方式有两种，一种是顺序寻址方式，另一种是跳跃寻址方式。

**顺序寻址​**​：通过程序计数器 PC 自增，自动形成下一条指令的访存地址。

*   PC 自增的大小与编址方式、指令字长有关，按字编址时每次自增 1。
*   若按字节编址，指令字长为 16 位，则 PC 自增为 +2；若指令字长为 32 位，则 PC 自增为 +4。

**跳跃寻址​**​：跳跃是指由本条指令给出下条指令地址的计算方式，**通过转移类指令实现程序的条件和无条件转移**。而是否跳跃可能受到状态寄存器的控制，跳跃的方式分为

*   直接转移：转移目标地址直接放在指令中，执行时**直接将地址码放入 PC**。
*   间接转移：转移目标地址存放在**寄存器**中。
*   绝对转移：地址码直接**指出转移目标地址**。
*   相对转移：地址码指出转移目的地址**相对于当前 PC 值的偏移量**。

CPU 总是**根据 PC 的内容去主存取指令**的，因此转移指令执行后修改 PC 值，下一条指令仍通过 PC 给出。

（2）数据寻址

数据寻址方式较多，通常在指令字中设置一个寻址方式字段区别寻址方式（其位数决定了寻址方式的种类）：

![](https://pic4.zhimg.com/v2-78d2b2ad7213d70e82c7da770ba71bb9_r.jpg)

CPU 根据操作数的寻址方式来区分存放在寄存器的是操作数还是操作数地址

指令中的地址码字段是**形式地址 A**，并不代表操作数的真实地址。形式地址结合寻址方式，可以计算出操作数在存储器中的真实地址，这种地址称为**有效地址 EA**。

*   若为立即寻址，则形式地址的位数决定了**操作数的范围**。
*   若为直接寻址，则形式地址的位数决定了**可寻址的范围**。
*   若为寄存器寻址，则形式地址的位数决定了**通用寄存器的最大数量**。
*   若为寄存器间接寻址，则**寄存器的位数决定了可寻址的范围**。

在计算机系统的指令表示中：

*   无括号 X：表示**寄存器/存储器的名称**，如 R0 是寄存器的名字，MAR 是地址寄存器的名字。
*   带一层括号 (X)：表示 **X 中的内容**，如 (R1) 表示寄存器 R1 中存储的数据；M(MAR) 表示 **MAR 内容所指的主存单元所存储的数据**。
*   带两层括号 ((X))：表示**以 X 中的内容为地址的寄存器中存储的数据**，即先取 X 的值作为地址，再找该地址对应的寄存器内容。

e.g.指令“add (R4), (R5)+”表示将 R4 内容所指存储器的数据与 R5 内容所指存储器的数据相加，并将结果送入 R5 内容所指存储器中：

*   “(R4)” 表示**寄存器间接寻址**，即操作数是寄存器 R4 的内容所指向的存储单元的值。
*   “(R5)+” 表示**寄存器间接自增寻址**，操作数是寄存器 R5 的内容所指向的存储单元的值，且 R5 的内容自增 1。

**隐含寻址**：指令不明显给出操作数地址，隐含操作数地址。有效**缩短指令字长简化地址结构**，但需增加存储操作数或隐含地址的硬件。

![](https://pic3.zhimg.com/v2-1dde9d780b2c616f25c304a2f86944cc_r.jpg)

**立即寻址**：指令字中的**形式地址 A 指出的是操作数本身**（采用补码表示）。

*   **操作数与指令一起放在主存**，取指令时操作数随指令一起被读取并送到 IR 中。
*   指令在**执行阶段不访存**，直接从 IR 取出操作数，执行速度最快。
*   但 A 的位数限制了立即数的范围，常用于**变量赋初值**。

**直接寻址**：指令字中的**形式地址 A 就是操作数的有效地址 EA**，即 EA = A。

*   **操作数与指令分开存放在主存**，执行指令需要 **1 次访存**获取操作数。
*   简单但 A 的位数限制了该指令操作数的寻址范围，操作数的地址不易修改。

**间接寻址**：指令字中的**形式地址 A 是操作数有效地址所在主存单元的地址**即 EA = (A)。**操作数与指令分开存放在主存**，这可扩大寻址范围，便于修改地址，但一次指令执行**需 2 次访存**执行速度较慢。

![](https://pic1.zhimg.com/v2-0bdc7c166baba5fe43d9aa7bd164ab56_r.jpg)

**寄存器寻址**：指令字中的**形式地址 A 是操作数所在 GPR 编号**，该 GPR **存储的即操作数**。

*   取指令时操作数不会随指令一起被读取并送到 IR 中，执行指令**只访问寄存器**。
*   有效缩短指令地址段位数且执行速度快延迟低，但寄存器数量有限不能存储大量的操作数。

**寄存器间接寻址**：指令字中的**形式地址 A 是操作数所在 GPR 编号**，该 GPR **存储的即操作数地址**。

*   **操作数与指令分开存放在主存**，扩大寻址范围便于共享和动态变化。
*   仅**需访存 1 次**，但速度较慢且指令执行相对复杂。

![](https://pic3.zhimg.com/v2-f81b4de6c321dd550c20f2d78a3ef1c8_r.jpg)

**基址寻址**：将基址寄存器 BR 内容加上指令格式中的形式地址 A 形成操作数的有效地址，即 EA = (BR)+A。其中基址寄存器既可以采用专用寄存器也可以某个通用寄存器。扩大寻址范围但偏移量位数较短，**一般由系统修改**，常用于**多道程序设计、内存分区和浮动程序**。

![](https://picx.zhimg.com/v2-babcf7ae17d09596f170e736d8227c8f_r.jpg)

**相对寻址**：把 **PC +1 后**的内容加上指令格式中的形式地址 A 形成操作数的有效地址，即 EA = (PC)+A，**操作数地址不固定随着 PC 值变化而变化**，便于程序浮动，广泛应用于**转移指令**。

*   形式地址 A 的值看作是偏移量 offset 以补码形式存在，目标地址 = 更新后 PC 值+offset。
*   若一条指令占 k 个地址单元，则目标地址 = 更新后 PC 值+offset\*k。

**变址寻址**：将变址寄存器 IX 内容加上指令格式中的形式地址 A 形成操作数的有效地址，即 EA = (IX)+A。其中变址寄存器既可以采用**专用或通用寄存器**，**基准值由用户主动控制**，扩大了寻址范围，面向用户且偏移量足够大，常处理**下标顺序访问数组和循环**。

![](https://pic3.zhimg.com/v2-0c3df53ddd67de88d2dcbf0d250f34f4_r.jpg)

以上三者的形式地址 A 即偏移量均固定不变，用补码表示

**堆栈寻址**：存储器（或寄存器组）中特定区域，按后进先出 LIFO 原则管理，其读/写单元地址由堆栈指针 SP 给出。**硬堆栈即寄存器**堆栈，成本高，不适合做大容量堆栈；**软堆栈从主存**划出区域做堆栈，合算且常用。

*   进栈：先压入数据 (A) -> M\_{sp} 后修改指针 (SP) - 1 -> A。
*   出栈：先修改指针 (SP) + 1 -> A 再弹出数据 M\_{sp} -> A。

tips：采用堆栈结构的计算机中，大部分指令表面为无操作数指令，操作数地址隐含使用 SP，读/写堆栈前后会自动对 SP 进行加减操作 。**子程序调用指令需要将程序断点保持至堆栈**，这个过程会**改变 SP 的值**，同时也会**改变 PC 的值以跳转到子程序执行**。

![](https://picx.zhimg.com/v2-4b9eb83cb830c0ab8a4ff3e82f83e7f5_r.jpg)

立即寻址和寄存器寻址不访存

*   **立即寻址**操作数获取便捷，通常用于给寄存器赋初值。
*   **直接寻址**相对于立即寻址，缩短了指令长度。
*   **间接寻址**扩大了寻址范围，便于编制程序，易于完成子程序返回。
*   **寄存器寻址**的指令字较短，指令执行速度较快。
*   **寄存器间接寻址**扩大了寻址范围。
*   **基址寻址**扩大了操作数寻址范围，适用于多道程序设计，内存分配。
*   **变址寻址**主要用于处理数组问题，适合编制循环程序。
*   **相对寻址**用于控制程序的执行顺序、转移等，适合转移指令。

e.g.设相对寻址的转移指令占 3B，第一字节为操作码，第二、三字节为相对位移量（补码表示），而且数据在存储器中采用以**低字节为字地址**的存放方式。每当 CPU 从存储器取出一字节时，即自动完成 PC+1→PC。

*   补码的扩充只需使用符号位补足即可，即正数补码的扩充只要补 0，负数补码的扩充只需补 1.
*   若 PC 的当前值为 240（十进制），要求转移到 290（十进制），即相对位移量为 47 = 2FH，正数在高位补 0，所以该转移指令的第二字节为 2FH，第三字节为 00H。
*   若 PC 的当前值为 240（十进制），要求转移到 200（十进制），即相对位移量为 -43 = D5H，负数在高位补 1，所以该转移指令的第二字节为 D5H，第三字节为 FFH。

### 3、常用汇编指令整理

（1）相关寄存器

x86 处理器有 8 个 32 位通用寄存器，为了向后兼容，EAX、EBX、ECX、EDX（E 表示 Extended 32 位）的高两位和低两位字节可以独立使用，如 EAX 低两字节称为 AX，而 AX 高低字节又可分别作为 AH、AL 8 位寄存器。

![](https://pica.zhimg.com/v2-9547bc1a9ed6b9be504945b136e4f9c6_r.jpg)

EIP 是 32 位指令指针寄存器即 PC 寄存器，它包含**下一条将执行指令的地址**。

汇编指令通常可分为数据传送指令、算术和逻辑运算指令和控制流指令，下面以 Intel 格式为例，介绍一些常用的指令。

（2）常用基础指令

以下用于操作数的标记分别表示寄存器、内存和常数。

<reg>：表示任意寄存器，其后带有数字则指定其位数。

*   <reg32> 表示 32 位寄存器（eax, ebx，ecx，edx，esi，edi，esp 或 ebp）。
*   <reg16> 表示 16 位寄存器（ax，bx，cx 或 dx）。
*   <reg8> 表示 8 位寄存器（ah，al，bh，bl，ch，cl，dh，dl）。

<mem>：表示内存地址（如 \[eax\]、\[var + 4\] 或 dword ptr \[eax + ebx\]）。

<con>：表示常数，<con8> 即 8 位常数；<con16> 即 16 位常数；<con32> 即 32 位常数。

**数据传输指令是最基本最常用的指令**，主要完成寄存器与寄存器、寄存器与存储器直接的传输：

mov 指令将第二个操作数（寄存器/内存中的内容或常数值）复制到第一个操作数（寄存器/内存）。

*   tips：双操作数指令的**两个操作数不能都是内存**，即 mov 指令不能用于直接从内存复制到内存。

```
mov eax, ebx          # 将 ebx 值复制到 eax
mov byte ptr [var], 5 # 将 5 保存到 var 值指示的内存地址的一字节中 
```

push 指令**将操作数压入内存的栈**，常用于函数调用。入栈前先将栈顶 ESP 值-4（**栈增长方向与内存地址增长方向相反**），然后将操作数压入 ESP 指示的地址。

*   语法：push <reg32>；push <mem>；push <con32> 。

```
push eax   # 将 eax 值入栈
push [var] # 将 var 值指示的内存地址的 4 字节值入栈
```

pop 指令执行的是**出栈工作**，出栈前先将 ESP 指示的地址中的内容出栈，然后将 ESP 值+4。

*   语法：pop <reg>；pop <mem>。

```
pop eax   # 弹出栈顶元素送到 eax
pop [ebx] # 弹出栈顶元素送到 ebx 值指示的内存地址的 4 字节中
```

lea 指令用于**获取内存地址**。

**算术和逻辑运算指令用于 CPU 实现最基本功能**，不同计算机对其支持有很大区别：

add/sub 指令将两个操作数相加/减，**结果保存到第一个操作数中**。

```
sub eax, 10            # eax ← eax - 10
add byte ptr [var], 10 # 10 与 var 值指示的内存地址的字节值相加，并将结果保存在 var 值指示的内存地址的字节中
```

inc/dec 指令分别表示将操作数自增/减 1。

```
dec eax             # eax 值自减 1
inc dword ptr [var] # var 值指示的内存地址的 4 字节值自加 1
```

imul 是**有符号整数乘法**指令，可以有两/三个操作数相乘，结果保存在第一个操作数中（必须为寄存器），注意乘法操作结果可能溢出，若溢出，编译器会置溢出标志 OF = 1，使 CPU 调出溢出异常处理程序。

```
imul eax, [var]    # 将 eax 寄存器的值与 [var] 所指向内存单元的值相乘，结果存回 eax ，即 eax ← eax * [var]
imul esi, edi, 25  # 将 edi 寄存器的值与立即数 25 相乘，结果存回 esi，即 esi ← edi * 25 
```

idiv 是**有符号整数除法**指令，只有一个操作数，操作结果有商和余数两部分，**商送 eax，余数送 edx**。

```
idiv ebx             # 用 edx 和 eax 组成的 64 位有符号整数除以 ebx 寄存器的值
idiv dword ptr [var] # 用 edx 和 eax 组成的 64 位有符号整数除以 [var] 所指向内存单元的值
```

and/or/xor/not 是位逻辑操作指令，**结果保存到第一个操作数中**。

```
xor eax, 0FH       # 将 eax 中的前 28 位全部置为 0，最后 4 位保持不变
xor edx, edx       # 将 edx 寄存器中的内容置为 0 
not byte ptr [var] # 将 var 值指示的内存地址的一个字节的所有位翻转，即 0→1 、1→0
```

shl/shr 是**逻辑左/右移**指令，neg 是取负指令。

```
shl eax, 1  # 将 eax 值逻辑左移 1 位
shr ebx, cl # 将 ebx 值逻辑右移 cl 位（n 为 cl 中的值）
neg eax     # 将 eax 寄存器中的值取负，即 eax ← - eax 
```

x86 处理器维持着一个指示当前执行指令的**指令指针 IP**，当一条指令执行后，此指针**自动指向下一条指令**。**IP 寄存器不能直接操作，但可以用控制流指令更新**。通常用标签 label 指示程序中的指令地址，在 x86 汇编代码中，可在任何指令前加入标签。例如：

```
       mov esi, [ebp+8]
begin: xor ecx, ecx
       mov eax, [esi]
```

这样就用 begin 指示了第二条指令，控制流指令**通过标签就可以实现程序指令的跳转**。

cmp/test 除了不改变操作数外等同于 sub/and，设备条件码。

jmp 无条件转移指令控制 IP 转移到 label 所指示的地址，从该地址取出指令执行。

计算机组成原理考研笔记六万字整理(4)
-------------------

```
jmp begin # 作用是转跳到 begin 标记的指令执行
```

jcondition 条件转移指令依据 CPU 状态字中的一系列条件状态进行转移。

*   je <label> ：相等时转移。
*   jz <label> ：上一个运算结果为 0 时转移。
*   jne <label> ：不相等时转移。
*   jg <label> ：大于时转移。
*   jge <label> ：大于等于时转移。
*   jl <label> ：小于时转移。
*   jle <label> ：小于等于时转移。

```
cmp eax, ebx
jle done      # 若 eax <= ebx，则跳转到 done 执行；否则执行下一条指令
```

call/ret 分别用于实现子程序（过程、函数等）的调用及返回，是程序调用中最关键的两条指令。

*   call 指令将**下一条指令的返回地址入栈**，然后无条件转移到由标签指示的指令，返回保存的地址。
*   ret 指令**弹出栈中保存的指令地址**，然后无条件转移到保存的指令地址执行。

（3）选择/循环语句的机器级别表示

​**​选择语句实现机制​**​：编译器借助条件码（标志位）设置指令以及各类转移指令实现选择结构语句。条件码反映最近算术或逻辑运算属性，可检测寄存器执行条件分支指令，常用条件码有 CF、ZF、SF 和 OF。常见算术逻辑运算指令如 add、sub 等会设置条件码，**comp 和 test 指令仅设置条件码不改变其他寄存器**。

​**​if - else语句​**​

![](https://picx.zhimg.com/v2-feb285bb10f9fc4efbe61524e8c5fcf9_r.jpg)

两个 return 顺序取决于条件转移指令的类型和方向，并非确定

p1 和 p2 是指针型参数，在 32 位机中长度为 dword ptr，比较指令 cmp 操作数来自寄存器，需先从栈取实参到寄存器，根据条件码组合执行指令，需条件转移指令。

```
mov eax,dword ptr [ebp+8]    # R[eax]←M[ebp+8]，即 R[eax]=p1
mov edx,dword ptr [ebp+12]   # R[edx]←M[ebp+12]，即 R[edx]=p2
cmp eax,edx                  # 比较 p1 和 p2，根据 p1 - p2 结果置标志
jbe .L1                      # 若 p1<p2，转标记L1处执行
mov eax,dword ptr [edx]      # R[eax]←M[edx]，即 R[eax]=M[p2]
jmp .L2                      # 无条件跳转到标记 L2 执行
.L1:
mov eax,dword ptr [eax]      # R[eax]←M[eax]，即R[eax]=M[p1]
.L2:
```

​**​循环语句实现机制​**​：编译器大多都统一转换使用 do-while 形式来产生机器代码，最后使用**条件转移指令**来判定循环条件的结束，**不一定包含无条件转移指令**。下面用汇编代码实现了一个简单的循环累加功能并将结果存入 eax 寄存器：

```
mov ecx,dword ptr [ebp+8]   # R[ecx]<-M[R[ebp]+8]，即 R[ecx]=n
mov eax,0                   # R[eax]<-0，即 result=0
mov edx,1                   # R[edx]<-1，即 i=1
cmp edx,ecx                 # Compare R[edx]:R[ecx]，即比较 i:n
jg .L2                      # If greater，转跳到 L2 执行
.L1:
add eax,edx                 # R[eax]<-R[eax]+R[edx]，即 result +=i
add edx,1                   # R[edx]<-R[edx]+1，即 i++
cmp edx,ecx                 # 比较R[edx]和R[ecx]，即比较 i:n
jle .L1                     # If less or equal，转跳到 L1 执行
.L2:
```

（4）过程调用的机器级表示

前面提到的 call/ret 指令主要用于过程调用，它们都属于一种**无条件转移**指令，一次过程调用对应一条调用指令，指令执行时必须保留返回地址，调用指令随后一条指令的地址即返回地址，**嵌套调用时通常保存在栈**中，**非嵌套调用时可保存在特定寄存器**内。

假定过程 P（调用者）调用过程 Q（被调用者），过程调用的执行步骤如下：

*   P 将入口参数（实参）放到 Q 能访问到的地方。
*   P 将返回地址存到特定的地方，然后将控制转移到 Q。（**call 指令实现**）
*   Q 保存 P 的现场（通用寄存器的内容），并与自己的非静态局部变量分配空间。
*   执行过程 Q。
*   Q 恢复 P 的现场，将返回结果放到 P 能访问到的地方，并释放局部变量所占空间。
*   Q 取出返回地址，将控制转移到 P。（**通过 ret 指令返回到过程 P**）

在上述步骤中，需要为入口参数、返回地址、过程 P 的现场、过程 Q 的局部变量、返回结果找到存放空间。且会**改变 SP 和 PC 的值**。

用户可见寄存器数量有限，调用者和被调用者共享寄存器，若直接覆盖对方的寄存器，则会导致程序出错。因此有如下规范：

*   寄存器 EAX、ECX 和 EDX 是**调用者保存寄存器**，当 P 调用 Q 时，若 Q 需用到这些寄存器，则由 P 将这些寄存到栈中，并在返回后由 P 恢复它们的值。
*   寄存器 EBX、ESI、EDI 是**被调用者保存寄存器**，当 P 调用 Q 时，Q 必须先将这些寄存器的内容保存在栈中才能使用它们，并返回 P 之前先恢复它们的值。

每个过程都有自己的栈区，称为栈帧，因此，一个栈由若干栈帧组成，寄存器 **EBP 指示栈帧起始位置**固定不变，寄存器 **ESP 指示栈顶**会随着数据的入栈而动态变化，栈从**高地址向低地址增长**，栈帧的范围在 EBP 和 ESP 指向的区域之间。

（5）汇编分析实例

![](https://pic2.zhimg.com/v2-b129a9bebce8dcd9b3439e9e35e0d831_r.jpg)

从左到右，四列分别为：

*   **行号**：最左侧的数字 1、11、12 等是代码的行序号，用于标识指令在代码中的位置，方便阅读和调试。
*   **虚拟地址**：中间的十六进制数 00401000、00401018 等表示对应指令在**虚拟内存空间**中的地址。程序运行时，CPU 通过虚拟地址来定位要执行的指令。
*   **机器码**：汇编指令左侧的十六进制数 55、83 7D 08 01 等是**机器码**，即汇编指令对应的二进制指令编码（这里以十六进制显示），是 CPU 实际执行的底层指令。
*   **汇编指令**：最右侧汇编指令 push ebp、cmp dword ptr \[ebp + 8\], 1 等是人类可读的指令助记符，描述了 CPU 要执行的操作。

### 4、CISC/RISC

当今指令系统朝着两个方向发展：复杂指令集 **CISC 增强原有指令功能**，设置复杂新指令实现软件功能硬化，典型如 x86 架构计算机；精简指令集 **RISC 减少指令种类和简化指令功能**，提高指令执行速度，典型如 ARM、MIPS 架构计算机。

（1）CISC 复杂指令系统计算机

集成电路技术发展使软件成本上升，促使在指令系统中增加复杂指令，形成了 CISC：

*   指令系统复杂庞大，指令数一般 200 条以上，功能尽可能强。
*   指令**长度不固定**、格式多、寻址方式多，**访存指令不受限**，**不利于编译优化**。
*   各种指令**使用频度差异大**，多数指令需多个时钟周期完成。
*   控制器**多采用微程序控制**，部分复杂指令无法采用硬连线控制。

传统 CISC 指令系统测试表明，**少数简单指令被反复使用**，占比约 80%，促使 RISC 诞生。

（2）RISC 精简指令系统计算机

核心思想是简化指令系统，多用寄存器 - 寄存器操作指令，指令格式力求一致：

*   选取高频简单指令，指令数一般小于 100 条，**复杂功能由简单指令组合实现**。
*   指令**长度固定**，格式种类少，寻址方式种类少。
*   仅 **LOAD/STORE 指令访存**，其余操作在寄存器间进行，**通用寄存器数量多**。
*   采用**指令流水线技术**，大部分指令一个时钟周期内完成。
*   以**硬布线控制为主**，少用微程序控制，重视编译优化。

tips：LOAD/STORE 指令是规整型指令系统中的一种**通用寄存器型指令风格**，唯一访存，代价是程序里访存指令变多、整体长度可能更长。

**CISC 软件兼容性好，RISC 简化后兼容性差但实用性强**。现代 CISC 结构 CPU 融合 RISC 成分，性能差距缩小，CISC 可提供更多功能满足程序设计需求，但 **RISC 和 CISC 之间不可能兼容**。

![](https://pica.zhimg.com/v2-a98fe8d0882ec745dfb322095b718d4e_r.jpg)

五、中央处理器
-------

### 1、CPU 的功能结构与分类

（1）CPU 的功能概述

中央处理器（CPU）由**运算器**和**控制器**组成。其中，控制器负责协调并控制计算机各部件指令序列，即**取指令、分析指令和执行指令即发出控制信号**；运算器的功能是**对数据进行加工**。CPU 的具体功能包括：

*   **指令控制**：完成取指令、分析指令和执行指令的操作，即程序的顺序控制。
*   **操作控制**：产生完成一条指令所需的操作信号，把各种操作信号送到相应的部件，从而控制这些部件按指令的要求正确执行。
*   **时间控制**：严格控制各种操作信号的出现时间、持续时间及出现的时间顺序。
*   **数据加工**：对数据进行算术和逻辑运算。
*   **中断处理**：出现异常情况和中断请求时，暂时中断当前程序并做处理，完成后返回断点继续执行。

指令执行的一般流程为：

![](https://pic4.zhimg.com/v2-0683112fa0126cdf4f67e6498307cc5f_r.jpg)

tips：**空操作指令**执行时，**PC 也会自动加 1**，因此有寄存器内容改变。

（2）CPU 基本结构

![](https://pic2.zhimg.com/v2-62d8062c1dd28ae3744a3a01328357b5_r.jpg)

指令译码器 **ID 位于控制器**中，仅**对操作码字段进行译码**，借以确定指令的操作功能。

CPU 中的寄存器按**汇编/机器程序是否可访问**，可分为两类：

*   **可见**：如 GPRs、PC、PSW、基址寄存器等，可编程以通过使用这类寄存器**减少访存次数**。
*   **透明**：如 MAR、MDR、IR、ACC、SR、暂存寄存器，不可编程且经控制部件来控制 CPU 的操作。

运算器中的寄存器：

*   **通用寄存器组 GPRs**：可编程指定多种功能的寄存器（**位数取决于机器字长**），用于存放**操作数和各种地址**信息等，如 AX、BX、CX、DX、SP 等，在指令中要指定寄存器的编号。
*   **累加寄存器 ACC**：它是一个通用寄存器，用于暂时存放 ALU 运算的结果。
*   **移位寄存器 SR**：可用来存放操作数，且在控制信号的作用下，寄存器中的数据可按需左右移位。
*   **暂存寄存器**：用于暂存从数据总线或通用寄存器送来的操作数，以便在取出下一个操作数时将其同时送入 ALU。
*   **程序状态字寄存器 PSW**：保留由**算术/逻辑运算或测试指令运行**结果而建立的各种状态信息，如 OF、SF、CF、ZF 等，每个标志位通常由一位触发器来保存，这些标志位组合在一起称为程序状态字，主要用于条件转移或条件设置指令的条件判断，不需要标号，**用户不能直接修改其值**。
*   **标志寄存器**：它是一个专用寄存器，不需要编号，也不能在指令中直接指定编号来访问，其标志位主要用于条件转移或条件设置类指令的条件判断，**用户不能直接修改其值**。

控制器中的寄存器：

*   **程序计数器 PC**：存放下一条执行指令在主存中的地址，其**位数通常同 MAR**；**决定程序的执行顺序**，当遇到转移类指令时，若成功则 PC 修改为转移指令的目标地址，否则是 PC **自增 1 个指令长度**后地址，**不显式使用**。
*   **指令寄存器 IR**：存放当前正在执行的指令，IR 的位数等于指令字长。
*   MAR、MDR 等。

e.g.若主存储器空间为 4GB，字长为 32 位，按字节编址，采用 32 位字长指令字格式。若指令按**字边界对齐存放**，则 PC 位数至少为 log\_2\\frac{4GB}{4B}=30 位，IR 位数至少为 32 位。

（3）指令周期

CPU 每**取出并执行一条指令所需的全部时间**称为指令周期。每周期都 **MAR→地址线，控制线发读/写信号，MDR←→数据线**。

![](https://pic2.zhimg.com/v2-010d839c6956dee54b6e20c8730d831f_r.jpg)

早期计算机为便于各指令同步，还将指令周期划分为若干个机器周期（从主存取出一个存储字所需最短时间）

**取指周期**：根据 PC 把下条指令地址→MAR→主存→MDR→IR **将指令存放 IR 中分析**，**CU 自动进行取址**，不同长度的取址指令的取址操作可能不同。

![](https://pic1.zhimg.com/v2-f6dcffdfa761a80e89f06a2daa772414_r.jpg)

取指周期的数据流向，不同长度的指令其取指操作可能不同

**间址周期**：间接寻址指令需要先从 IR **将指令中的地址码**→MAR→主存，此后 CU 向主存发出读命令，将**操作数的有效地址**→MDR→MAR，然后再次启动读操作访问主存，取出操作数。

![](https://pica.zhimg.com/v2-b95cb1ffe016e45e3214052e468531ca_r.jpg)

一次间址周期的数据流向，与二次间址、多次简址的步骤不同

**执行周期**：取操作数，根据 IR 指令字的操作码通过 ALU 执行结果，无统一数据流向。

**中断周期**：CPU 在每条指令执行结束前都要发中断查询信号，若有中断请求，则 CPU 进入中断响应阶段处理中断请求。若程序断点存入堆栈，SP 指示栈顶地址，则 **CU 控制 SP-1，SP 流入 MAR**。

![](https://pic4.zhimg.com/v2-1970819fa22f8bfa0b03a04154011a03_r.jpg)

中断周期的数据流向，保存 PC、PSW

tips：CPU 控制器根据**指令周期的不同阶段**来区分是指令还是数据。

*   JMP 无条件转移不访存，**没有间址周期**，整个周期 **PC 值修改两次**。
*   DMA 是直接由外设通过 DMA 控制器和内存读写的，**每次 DMA 传输占用一个存取周期**。

不同的处理器采用不同的指令执行方案来安排指令执行的顺序：

*   ​**​单周期处理器​**​：每条指令 **CPI = 1 相同**，指令间串行执行，时钟周期以最耗时指令用时为准，所以 CPU 时钟频率较低，指令执行过程**每个控制信号保持不变，部件只使用一次**。
*   ​**​多周期处理器​**​：针对不同类型指令选用不同执行步骤，每条指令 **CPI > 1 可变**，指令间串行执行，各时钟周期长度均匀一般**比单周期处理器短**，**控制信号可能改变，同一部件可使用多次**。
*   ​**​流水线处理器​**​：指令间**并行**执行，理想情况 CPI = 1，使多条指令同时处于不同执行步骤中运行。

（4）多处理器认识

依据指令流与数据流数量，计算机体系结构分为：

*   **SISD​​**：传统串行计算机结构，含一个处理器和一个存储器，处理器串行执行指令，部分采用流水线、多模块交叉存储方式。
*   ​**​SIMD​**​：一个指令流同时处理多个数据流，由指令控制部件和多个处理单元组成，各单元有独立地址寄存器处理不同数据。**适用于 for 循环数组**处理，对 switch 语句效率低。
*   **MIMD​**​：同时执行多条指令处理多个不同数据，分多计算机系统（各节点**独立存储**和**主存地址空间**，靠**消息传递**数据）和多处理器系统（**共享单一地址空间**，靠**存取指令访问**存储器）。

**阵列处理机、向量处理器是 SIMD 结构**，**SIMD 和 MIMD 是并行计算模式**，前者为数据级并行，后者为线程级或更高级并行计算模式。**常规单处理器为 SISD，常规多处理器为 MIMD**。

传统 CPU 线程切换开销大影响系统性能，因此引入**硬件多线程（单/多核均可）**，这类 CPU 需为**每个线程配备单独 GPRs、PC** 等，线程切换时仅激活选中寄存器，省略存储器数据交换环节，降低切换开销。

*   ​**​细粒度多线程​**​：多个线程轮流交叉执行不相关指令，可**乱序并行**，处理器每个时钟周期切换线程，如时钟周期 i 执行线程 A 指令，i + 1 执行线程 B 指令。
*   ​**​粗粒度多线程​**​：连续多个时钟周期执行同一线程指令序列，仅在当前线程出现如 Cache 缺失等较大开销阻塞时切换线程，发生流水线阻塞时需清除和重载流水线，线程切换开销比细粒度大，上述**两种技术实现指令级并行**，但线程级不并行。
*   ​**​同时多线程 SMT​**​：细粒度与粗粒度多线程技术的变体，实现指令级和线程级并行，在同一时钟周期发射多个不同线程中的多条指令执行。**Intel 超线程即 SMT 共享 Cache 和功能部件**。
*   **超线程 HT**：在一个 CPU 中提供两套线程处理单元，实现**线程级并行**，共享 Cache 和功能部件，但两个线程同时需某资源时，一个线程需挂起，性能不等于两个 CPU 性能；且需软硬件和系统支持。
*   **双核技术​**​：将两个一样的 CPU 核心集成到一个封装内，实现空间并行，还能提高功耗和减少体积。

![](https://pic1.zhimg.com/v2-c778b46b23a445ab1ffc29b94454c5f6_r.jpg)

多核处理器将多个处理单元（核）集成于单个 CPU，各核可**有自己的 Cache 也能共享**，且通常**共享主存**。

*   **多核​**​：在多核计算机系统中，为充分发挥硬件性能需采用多线程或多进程执行，使各核同时有线程执行，其多个线程在物理上并行，**主频相同降低功耗和体积**，真正意义上同时执行多个线程。
*   ​**​单核​**​：单核上的多线程是多线程并发交错执行，同一时刻实际仅一个线程在执行。

![](https://pica.zhimg.com/v2-5e4953475b76ba6214155b8245649468_r.jpg)

不共享 Cache 的双核 CPU 结构，每个核含 CPU 状态、中断逻辑、执行单元和 Cache

**共享内存多处理器 SMP** 是具有**单一物理地址空间**、处理器间通过共享变量通信且可**在虚拟地址空间单独运行程序**的多处理器。其按存储器访问特性分为两种类型：

*   ​**​统一存储访问 UMA​**​：各处理器对存储单元访问时间大致相同，早期计算机采用多，内存控制器未集成到 CPU，访存经北桥芯片和前端总线。但因 CPU 性能提升方式转变，前端总线成瓶颈。
*   ​**​非统一存储访问 NUMA**​：部分存储器访存速度因处理器和访问位置而异，因主存被分割分配给不同处理器，每个 CPU 有独立内存控制器和本地内存，通过 QPI 总线连接并可访问远程内存，访问存在本地和远程区别。

在 SMP 中，**多处理器同时访问共享变量需同步**，常用**加锁**方式。在 UMA 架构多处理器中，多核系统 Cache 一致性包括 Cache 与内存及各 CPU Cache 间的一致性，即不同 CPU 对内存同一位置数据的 Cache 内容应一致。

### 2、数据通路的组成与实现

CPU 内部结构逻辑功能虽复杂，但都可视为由**数据通路**和**控制部件**两大部分组成。

（1）数据通路的组成

数据通路指数据在指令执行过程中所经**路径及路径上部件**，如 ALU、通用寄存器、异常检测和响应电路等，描述信息流向，**不包含控制部件**。CPU **读写控制信号线决定了是从存储器读还是向存储器写**。

组成数据通路的元件主要分为：

*   ​**​组合逻辑元件（操作元件）​**​：任何时刻输出仅取决于当前输入，无存储信号记忆单元，不受时钟信号控制。常见有**加法器、ALU、译码器、多路选择器、三态门**等。
*   ​**​时序逻辑元件（状态元件）​**​：某时刻输出与该时刻和以前输入都有关，必含存储信号的记忆单元，须在时钟节拍下工作。常见有**通用寄存器、程序计数器、堆栈/状态存储器**等。

tips：译码器 n 位输入对应 2^n 种输出；多路选择器需控制信号 Select 来确定选择哪个输入被输出；三态门由控制信号 **EN=1 时打开**，输出信号等于输入信号，**EN=0 时输出端呈高阻态**，所连寄存器与总线断开 。

数据通路的基本结构主要有以下几种：

**CPU 内部单总线方式​**​：将 ALU 及所有寄存器连接到一条内部总线上，结构简单但存在**较多冲突**，性能较低。

![](https://pic2.zhimg.com/v2-9726efcdd2b4d24402bab01551998dcd_r.jpg)

各部件输出端 out 部分均连接三态门来控制

X 为暂存器，ALU 的两个操作数 A 和 B 都需要通过单总线来传送，由于单总线同一时间只能传输一个数据，A 和 B 无法同时传送，否则会造成数据冲突。所以 A 和 B 需要**分时通过单总线传送给 ALU**，这就需要**暂存器 X 来暂存其中一个操作数**，以保证 ALU 能获取到所需的两个操作数进行运算。

tips：**单周期 CPU 不能采用单总线方式**，因为单总线将所有寄存器都连接到一条公共总线上，一个时钟内只允许一次操作，无法完成一条指令的所有操作。ALU 只有**一个输入端与总线相连**，**另一输入端以及输出端都要通过暂存器与总线相连**。

**CPU 内部多总线方式**：将所有寄存器的输入端和输出端都连接到多条公共通路上，提高效率。

tips：该方式总线上每个时刻只有**一个器件发出信息**（否则会总线冲突），可以有**一或多个器件接收**。

**专用数据通路方式**：根据指令执行过程中的数据和地址的流动方向安排连接电路，避免使用共享的总线，结构复杂硬件量大性能较高，基本不存在数据冲突现象。

（2）数据通路的操作实例

总线是一组共享的传输信号线，它**不能存储信息**，任一时刻也**只能有一个部件把信息送到总线**上。我们以 MDR 为例实际上有三种控制信号线：

*   MDRin：MDR 从**内部总线**接收数据，如 AC、IR 等部件通过内部总线送数据到 MDR。
*   MDRout：MDR 向**内部总线**输出数据，如 MDR 将数据送到 IR、A 等部件。
*   MDRinE：MDR 从存储器通过 **DB 总线**接收数据，如存储器读操作 M→MDR。

下面以单总线数据通路为例分析常见操作流程及控制信号~

**通用寄存器之间传送数据**：在寄存器和总线之间有两个控制信号 Rin 和 Rout。

*   当 Rin 有效时，控制将总线上的信息存到寄存器 R 中。
*   当 Rout 有效时，控制将寄存器 R 的内容送至总线。

**从主存读取数据**：如 CPU 根据 PC 值从主存中取指令。

![](https://pic4.zhimg.com/v2-66751c86d3f4ffc36f64186db3a8015d_r.jpg)

三个步骤分别需要一个时钟周期、主存周期、时钟周期

**将数据写入主存**：将寄存器 R1 的内容写入寄存器 R2 所指的主存单元。

![](https://pic3.zhimg.com/v2-35803fc5b48599a7592343b5a8f8e40c_r.jpg)

**执行算术或逻辑运算**：加法指令 ADD ACC, R1实现将 ACC 内容与 R1 内容相加并写回 ACC。

![](https://pic3.zhimg.com/v2-e0767a907c6a53e4c5410a4a077a0d4e_r.jpg)

Y 为暂存器，该操作三步不能同时执行否则会总线冲突，需要三个时钟周期

**修改程序计数器的值**：转移指令通过对 PC 中的数值进行修改实现转移的目的。假设转移指令 JMPaddr 实现 IR 中的地址字段值送入 PC，完成该操作的流程及控制信号为 IRout 和 PCin 有效，数据通路不仅影响 CPU 内部微操作形成的控制信号，还影响指令执行过程的微操作序列的安排。

e.g.获取指令 ADD (R1), R0 然后执行其对应功能 (R0) + ((R1)) → (R1) 步骤，具体实现流程分为**取指阶段 C1~C4 和执行阶段 C5~C9** 如下分析：

*   C1：将 PC 的内容送入 MAR 指定下一条指令的地址。
*   C2：以 MAR 内容为地址，读取存储器数据送入 MDR，PC 自增 1 指向下一条指令地址。
*   C3+C4：MDR 中的指令数据送入 IR，指令译码部件解析 IR 中的 ADD (R1), R0 为执行做准备。
*   C5：将 R1 内容作为地址送入 MAR，注意 ((R1)) 是间接寻址方式。
*   C6：以 MAR（R1 内容）为地址，读取存储器数据，R0 的内容送入暂存器 A。
*   C7：MDR 中的 ((R1)) 与 A 中的 (R0) 通过 ALU 做加法，结果送入 AC。
*   C8+C9：AC 中的加法结果送入 MDR，MDR 中的结果写入以 MAR（R1 内容）为地址的存储器单元。

![](https://pic2.zhimg.com/v2-ab24273f2d1140ba7ee6a79493e28291_r.jpg)

### 3、控制器的结构和功能

（1）控制器认识

![](https://pic3.zhimg.com/v2-4879ed193423bb3440eb7e5f7e52ebf8_r.jpg)

计算机硬件系统和控制器部件的组成

控制器 CU **从数据总线接收指令信息**，**从运算器部件接收指令转移地址**，送出指令地址到地址总线，还要向系统中的部件提供它们运行所需要的控制信号，是计算机系统的指挥中心，主要功能有：

*   从主存中取出一条指令，并指出下一条指令在主存中的位置。
*   对指令进行译码或测试，产生相应的操作控制信号，以便启动规定的动作。
*   指挥并控制 CPU、主存、输入设备和输出设备之间的数据流动方向。

tips：**指令寄存器 IR 与标志寄存器 FR 的输出信号连接到 CU 的输入端**。

控制器**在 CPU 内部，用 ROM 实现**，可分为**硬布线**控制器和**微程序**控制器：

*   两类控制器中的 **PC 和 IR 是相同的**，都**存放实现指令系统的所有微指令**，均按其**地址访问**。
*   但确定和表示指令执行步骤的办法及给出控制各部件运行所需要的**控制信号的方案是不同**的。

（2）硬布线控制器

硬布线控制器由复杂的组合逻辑门电路和触发器构成，也称**组合逻辑控制器**，增删修改质量需要重新设计电路，调试困难，**一般用于 RISC**。

存放在 IR 的 n 位操作码经过译码电路产生 2^n 个输出，送至 CU 发出不同的微操作控制信号。

![](https://pica.zhimg.com/v2-f2a5b85160b42a3798084b2d6e678fd6_r.jpg)

将指令译码器和节拍发生器从 CU 中分离得到简化的控制单元框图

CU 的输入信号来源有：经指令译码器译码产生的**指令信息**、**时钟脉冲**、执行单元的**状态标志**。

（3）微程序控制器

微程序的设计思想就是**将每条机器指令编写成一个微程序**存储在一个控制存储器中，每个微程序包含若干微指令，每条微指令对应一个或几个微操作命令。目前，大多数 CISC 计算机都采用微程序设计技术，**机器指令和微指令都可以直接被 CPU 执行**。

**微命令**：在微程序控制的计算机中，控制部件向执行部件发出的各种控制命令，是控制序列的**最小单位**，执行部件收到微命令后进行**微操作**（一一对应）。硬布线控制器也有这概念，非微程序独有。

*   相容性微命令是指那些可以同时出现、共同完成某一些微操作的微命令，**分在同一段**。
*   互斥性微命令是指在机器中不允许同时出现的微命令，**分在不同段内**。

**微指令**：若干微命令的集合，一条微指令通常至少包含两大部分信息：

*   操作控制字段，也称**微操作码**字段，用于产生某一步**操作所需的各种操作控制信号**。
*   顺序控制字段，也称**微地址码**字段，用于控制产生**下一条要执行的微指令地址**。

**微周期**：从控制存储器中**读取并执行一条微指令**所需要的全段时间，通常为**一个时钟周期**。

**微地址**：控制存储器 CM 的单元地址。**机器指令的操作码字段指出各微程序入口地址**。

*   微地址寄存器 μMAR：用于存放待执行微指令在 CM 中的微地址。
*   微指令寄存器 μIR：用于存放从 CM 中读出的微指令，位数即 CM 中存储的每条微指令的位数。
*   微程序计数器 μPC：决定微指令的执行顺序，其位数 n 决定了能指向的微指令数量 2^n 。
*   控制存储器 CM 的容量就是**微指令数量乘以每条微指令的位数**。

![](https://pic4.zhimg.com/v2-e98c1546bb0df5d3db6bcfefedeb5071_r.jpg)

微程序控制器的基本结构

微程序控制器的工作过程即计算机执行机器指令的过程，具体如下：

*   ​**​取指令公共操作​**​：将取指微程序入口地址（通常为 CM 的 0 号单元）送入 μPC，相应微指令 CM→μIR，完成后机器指令从 MDR→IR。
*   ​**​生成微地址​**​：机器指令的操作码经微地址形成器，产生对应微程序的入口地址，并送入 μPC。
*   ​**​执行微指令​**​：从 CM→μIR→控制信号中逐条取出对应的微指令并执行。
*   ​**​循环操作​**​：执行完一条机器指令对应的微程序后，回到取指微程序入口地址，重复直至整个程序执行完毕。

tips：通常一条机器指令对应一个微程序，由于**任何机器指令的取指令操作相同**，可将其微命令编成专门微程序，负责取指令送指令寄存器，还可编写对应间址周期和中断周期的微程序，这样 CM 中**微程序个数为机器指令数加上取指、间址和中断周期等公共微程序数**。

后继微地址形成的基本类型如下：

1.  **断定法​**​：微指令格式中设**后继地址字段直接指出后继微指令地址**。
2.  **增量法​**​：(μPC)+1→μPC，**适用于后继微指令地址连续情况**。
3.  **指令法​**​：机器指令取自 IR 后，操作码经微地址形成部件输出对应微程序首地址。
4.  **标志法​**​：根据各种标志决定下一条微指令分支转移的地址。
5.  **硬件法​**​：电源加电后硬件电路产生第一条微指令地址并送至 μPC，作为取指周期微程序入口地址。

微指令的编码方式是微指令的控制方式，旨在**保证速度时缩短微指令字长**，主要有以下三种：

*   ​**​直接编码控制方式​**​：操作控制字段**每位代表一微命令**，设置 1 选用 0 不选用某微命令。简单直观不用译码、执行速度快、操作并行性好，但微指令**字长过长**，**控制存储器容量大**。
*   ​**​字段直接编码方式​**​：将操作控制字段分成若干小字段独立编码，每个小段留**一个状态表示本字段不发微命令**。能**缩短微指令字长**，但因需译码电路发出微命令，所以比直接编码方式**慢**。
*   ​**​字段间接编码方式​**​：一个字段的某些微命令由另一个字段中的某些微命令解释。进一步缩短微指令字长，但削弱了微指令的并行控制能力，通常作为字段直接编码方式的辅助手段。

微指令格式与微指令编码方式有关，常分为水平型和垂直型：

**水平型微指令**包括上述三种编码。**并行能力强执行速度快**、微程序短**但微指令长**，编写微程序麻烦。

![](https://pica.zhimg.com/v2-b26502c13f63cb1fbaf15da00e7e674c_r.jpg)

指令字一位对应一个控制信号，有 1 无 0 输出，一条指令定义并执行多个并行操作微命令

**垂直型微指令**采用类似机器指令操作码方式，在微指令字中**设置微操作码字段**。微指令短简单规整，便于编写微程序但长，执行速度慢效率低。

![](https://pica.zhimg.com/v2-58a0c63c4e00f3721f9952f89743ead6_r.jpg)

一条指令只能定义并执行一种微命令

![](https://pic4.zhimg.com/v2-c7a17c1947f2f4cdbbbfb4da5921b2fd_r.jpg)

微程序控制中，CM 存有微指令在执行时需要读出，增加时间消耗因而慢

### 4、异常与中断机制

（1）异常与中断分类

*   **异常（内中断）**：CPU 执行一条指令时，在其自身内部检测到、与正在执行的**指令相关**的**同步**事件。
*   **中断（外中断）**：由 CPU 外部设备请求触发、与正在执行的**指令无关**的**异步**事件，不阻止指令完成。

处理过程：若 CPU 在执行用户程序的第 i 条指令时**硬件检测**到一个异常事件/中断请求信号，会**打断当前程序并设置断点**，然后转去**执行相应的异常或中断处理程序**（两者基本相同，操作系统完成）。

*   若处理程序能够解决相应的问题，则在最后的异常或中断**返回指令**，回到断点继续执行。
*   若异常或中断程序发现是不可恢复的致命错误，则**终止**用户程序。

异常分为硬故障中断和程序性异常，前者由硬连线出现异常引起的，后者也称软件中断，是指在 CPU 内部因执行指令而引起的异常事件。按异常发生原因和返回方式的不同可分为：

*   **故障**（Fault）指在引起故障的**指令启动后和结束时**被检测到的异常事件，如**缺页、除 0、非法指令**等，内核程序可以修复。断点为**当前发生异常的指令的地址**。
*   **自陷**（Trap）是由访管或陷入指令引发的人为异常，如**系统调用、调试断点、单步执行**等。断点为**访管或陷入指令下一条指令的地址**。
*   **终止**（Abort）是在执行指令过程中**随机发生**了硬件故障，如**控制器出错、存储器校验错、总线错误**等，内核程序也无法修复，需调出异常服务程序来重启系统。断点可以是**当前指令或下一条指令的地址**。

tips：终止、非屏蔽中断和外中断属于硬件中断，故障和自陷属于程序性异常。

**外中断**其请求信号在**一条指令执行完、取下一条指令前检测**。若检测到请求，响应中断并转至中断服务程序执行后，需返回原被中断程序中**已执行完指令的下一条指令执行**，而非刚执行完的指令。

（2）中断屏蔽

中断分为可屏蔽和不可屏蔽，常见有 I/O 中断、时钟中断、键鼠输入、DMA 传送等。

*   ​**​可屏蔽中断​**​：通过 INTR 向 CPU 发出请求，可在中断控制器设置屏蔽字屏蔽。
*   ​**​不可屏蔽中断​**​：经 NMI 向 CPU 发出，通常为紧急硬件故障需 **CPU 无论开关中断都处理**。

一台计算机允许有多个中断源，它们有**不同的中断处理程序**、**唯一类型号**和**随机的中断请求时间**。需设置优先级，分为响应和处理两类：

*   **响应优先级对多中断源**，由**硬件或查询程序顺序决定**不可改变。
*   **处理优先级对多重中断**，可利用**中断屏蔽技术动态调整**以实现。
*   中断屏蔽字 1 表示不可被中断，0 表示可被中断，**1 越多处理优先级越高**。
*   每个中断源设置中断请求标记触发器，状态为 **1 表示有请求**。
*   每条**指令执行结束时 CPU 统一扫描**各中断源检查有无中断请求。

e.g.某计算机有五级中断 \\(L\_4 \\sim L\_0\\) ，中断屏蔽字为 \\(M\_4M\_3M\_2M\_1M\_0\\) ， \\(M\_i=1\\) （ \\(0 \\leq i \\leq 4\\) ）表示对 \\(L\_i\\) 级中断进行屏蔽。若中断**响应优先级从高到低**的顺序是 \\(L\_4 \\to L\_0 \\to L\_2 \\to L\_1 \\to L\_3\\) ，求 \\(L\_1\\) 的中断处理程序中设置的中断屏蔽字。

\\(L\_1\\) 只能屏蔽 \\(L\_3\\) 和其自身，故 \\(M\_3\\) 和 \\(M\_1\\) 置 1 表示**不检测两者请求不被它们中断**，因此设 01010。

**优先级高的中断优先处理**，而非请求顺序，一般来说不可屏蔽中断>可屏蔽中断；在 I/O 传送类中断请求中高速设备>低速设备，输入设备>输出设备，实时设备>普通设备。

（3）异常和中断响应过程

CPU 执行指令时，若发生异常或中断请求，响应中断必须满足以下条件：

*   **中断请求**触发器处于 1 状态保持信号，且 CPU 允许**开中断**（**检测到请求即响应**）。
*   外设**中断允许 IF** 触发器处于 1 状态保持信号。
*   **当前指令执行完毕**，**无更高优先级任务**。

然后需**由 CPU 硬件执行中断隐指令（不在指令系统）**作出异常和中断响应：

*   ​**​关中断​**​：为防保存断点和程序状态期间被新中断打断，**设置中断允许触发器为 0** 禁止响应新中断。
*   ​**​保存断点和程序状态​**​：将程序断点（返回地址）、PC 和 PSW 压入栈中，以支持嵌套及恢复执行。
*   ​**​识别并转对应处理程序​**​：软件识别是 CPU 设异常状态寄存器，操作系统用**查询程序按优先级检测**异常和中断类型并**转内核**处理程序；硬件识别即向量中断，**中断向量是中断处理程序的入口地址**，存在中断向量表中，根据中断向量对应的类型号快速找处理程序。

完成以上中断响应后**操作系统启动中断服务程序**并执行，整个过程不可打断：

1.  **保护现场**：操作系统保存当前程序的**寄存器状态和中断屏蔽字**等。
2.  **中断事件处理**：执行具体的中断服务逻辑。
3.  **恢复现场**：恢复保护的寄存器状态，由**关中断指令完成**。
4.  **开中断**：为后续中断响应做准备，此时**每条指令执行结束时采样中断信号，检测到就立即响应**。
5.  **中断返回**：执行最后的返回指令，自行直接返回原程序。

中断请求和响应信号是在 I/O 总线的**控制线**上传送的，CPU 响应后是在**数据线**上获取中断类型号的。

*   **单重中断**：CPU 执行中断服务程序时，出现了新的**优先级更高的中断请求但不予响应**。
*   **多重中断**：**保护现场时关中断**，执行**中断处理程序时开中断**，遇到更高优先级的中断，CPU 会转去**处理新的中断请求**，各断点递归压入栈保存。

![](https://pica.zhimg.com/v2-5278d9bf480eb655a519a083ddc74d82_r.jpg)

### 5、指令流水线

前面介绍的都是单周期处理机采用串行执行指令，同一时刻 CPU 仅执行一条指令，功能部件使用率低。现代计算机普遍运用指令流水线技术，**同一时刻多条指令在 CPU 不同功能部件并发执行**，效率显著提高。

（1）指令流水线认识

提高并行性的两方面技术：

*   ​**​时间上​**​：将任务分解为不同子阶段，在不同功能部件并行执行，即**流水线技术**。
*   ​**​空间上​**​：在处理机内设置多个执行相同任务的功能部件并让其并行工作，对应**超标量处理机**。

指令级别的并行技术：

*   ​**​多发射技术​**​：采用多个内部功能部件，使**流水线功能段同时处理多条指令**，处理机一次可发射多条指令进入流水线执行，CPI < 1 但成本更高控制更复杂。
*   ​**​超流水线技术​**​：通过增加流水线级数，CPI = 1 使更多指令在流水线中重叠执行。
*   ​**​超长指令字技术​**​：也称静态多发射技术，编译程序将多条能并行操作的指令组合成超长指令字，对**优化编译要求更高且需要更多硬件资源**。
*   **超标量流水线技术**：也称动态多发射技术，每个**时钟周期内可并发多条独立指令**，并行操作执行两条或多条指令，需配置多个功能部件。多数超标量 CPU 结合动态流水线调度，指令**乱序执行**。

一条指令执行过程可分解为若干子任务，由相应子功能部件完成，**各阶段（子功能部件+流水段寄存器）视为流水段**，流水线中**各子任务耗时相同**，构成指令流水线。

计算机组成原理考研笔记六万字整理(5)
-------------------

*   取指（IF）：根据 PC 值从指令存储器或 Cache 取指令。
*   译码（ID）：产生控制信号，同时从寄存器中取数据。
*   执行（EX）：执行运算或计算地址。
*   访存（MEM）：对存储器读/写。
*   写回（WB）：将执行结果写回寄存器堆。

**各子功能都需要包含在某个流水段**中，每条指令都会**依次进入所有流水段**中处理。我们使第 k+1 条指令的译码与第 k 条指令的执行同时进行：

![](https://pic3.zhimg.com/v2-de0081274d22eba85a515549b6dd9fb2_r.jpg)

一个 5 段指令流水线，每条指令的时钟周期数 CPI 均为 1

（2）流水线的基本实现

流水线时钟周期设计原则：指令流水段个数和长度依**最复杂指令功能段个数和最长时间 + 流水段寄存器延时**定，此时其他指令可以通过**加入空操作功能段**向最复杂指令靠齐。

e.g.某指令 5 阶段时间分别为取指 200ps、译码 100ps、执行 150ps、访存 200ps、写回 100ps，总执行时间 750ps。按设计原则流水段长 200ps，每指令执行时间 1ns。

N 条指令时，单周期处理机用时 N×750ps，流水线处理机用时 (N+4)×200ps，提高程序整体执行效率。

tips：流水线填充延迟，**第一条指令需要经过所有 5 个阶段，但后续指令可以逐步进入流水线**。

各流水段还设**流水段寄存器锁存本段结果**，供**下段使用**，寄存器和数据存储器用**统一时钟 CLK 同步**，每来一时钟，各段处理数据锁存到段尾寄存器作为后段输入，当前段也会接收前段传递数据。

![](https://pica.zhimg.com/v2-3a97ea3f94a97ecd0ffa83de9a6028fc_r.jpg)

流水线逻辑结构图，五条不同指令并行执行

在流水线数据通路中，**时钟信号作用在 PC、各类寄存器、存储器等状态元件上**，PC 值无阻塞时每个时钟周期都会变。每条指令的**取指和译码阶段的功能都相同**，是公共流水段，且控制信号是指令译码之后才产生的，因此这两个阶段**不需要控制信号**。

![](https://pic1.zhimg.com/v2-4564b7576641b90befdd6ce59e4c8f0a_r.jpg)

一个 5 段指令流水线的时空图

适当增加流水段的个数，会使得每个流水段内的**操作更简单**，**延迟更小**，**缩短了时钟周期**，从而可以**提高时钟频率**。但是之间的流水段寄存器也增多，增加了流水段之间的**额外缓存开销**，控制**逻辑更复杂**。

（3）流水线的冒险和处理

在指令流水线中可能遇到问题使得后续指令无法正确执行而引起流水线阻塞，称为**流水线冒险**。

![](https://pic2.zhimg.com/v2-7bc9837337ac3b5273a010063b3bdc5b_r.jpg)

不同类型指令在各流水段中的操作，ALU 无访存段，转移指令无写回段

**结构冒险**：不同指令在同一时刻争用同一功能部件形成的冲突，如指令和数据通常存于同一存储器。

*   **暂停后续指令方法**：前一指令访存时，让后续指令暂停一个时钟周期。
*   **设置多个独立部件​**​：对寄存器访问冲突可将**寄存器的读写接口独立**；对于访存冲突，单独设置数据存储器和指令存储器，或将**数据 Cache 和指令 Cache 分离**的方式避免资源冲突。

![](https://pic2.zhimg.com/v2-c1a6b8f2c8d318020c0a2375bd635161_r.jpg)

用暂停后续指令的方式解决访存冲突

**数据冒险**：后面指令用到前面指令写结果之前的结果，在非乱序执行的流水线中发生**先写后读 RAW 冲突**。所有数据冒险**都能通过调整指令顺序和插入 nop 指令解决**。

![](https://pic2.zhimg.com/v2-7d791f56ad6971653baf269594692251_r.jpg)

add 和 sub 指令发生 RAW 冲突，在按序执行的流水线中只可能出现这种冲突

*   ​**​软件插入 nop ​**​：如可在 sub 指令前**插入 3 个 nop 指令**即暂停 3 个时钟周期直至 add 指令结果生成。
*   ​**​硬件阻塞 stall ​**​：也可通过硬件实现类似暂停效果，该周期内流水线并无执行任何有用工作。
*   ​**​寄存器读写操作重叠​**​：将寄存器的写口和读口分别控制在前、后半个时钟周期内操作。
*   ​**​转发旁路技术​**​：设置相关转发通路，不等前一条指令把计算结果写回寄存器，下一条指令也不再从寄存器读，而将数据通路中生成的**中间数据直接转发到 ALU 输入端**。

![](https://pic3.zhimg.com/v2-e4d1671b8919bd0998f3d04b80af7ec2_r.jpg)

使用延迟相关指令解决 RAW 冲突

![](https://pic1.zhimg.com/v2-f63739302b4247a3eb74c791dd700426_r.jpg)

使用转发技术解决 RAW 冲突

tips：**乱序执行**下可能发生读后写 WAR 或写后读 RAW。

​**​load - use 数据冒险**：前一条指令为**访存**将数据存入存储器，后一条指令使用该数据，**无法通过转发技术解决**的情况，至少会**引起一个时钟周期的阻塞**。可以通过**插入 nop 指令**或程序**编译时优化调整指令顺序**。

![](https://pic4.zhimg.com/v2-a9ba61cb21246fed6f92ba656860ef6d_r.jpg)

使用延迟加转发技术来解决 load-use 冲突

**控制冒险**：遇**转移或返回指令**、发生**中断或异常**时会改变执行顺序致控制冲突，如执行转移指令会改变 PC 值造成断流，至少会**引起一个时钟周期的阻塞**。

*   ​**​插入 nop 或硬件 stall**：对于由转移指令引起的冲突，可以在分支指令后推迟后续指令的执行。
*   ​**​分支预测​**​：可尽早生成转移目的地址，静态预测技术每次结果相同。动态预测技术根据局部性原理，其**预测成功率通常比静态预测技术高**。预测错误时，已被错误放入流水线执行的指令必须**被舍弃**。

tips：Cache 缺失的处理过程也会引起流水线阻塞。

（4）流水线的性能指标

**流水线的吞吐率**：单位时间内流水线所完成的指令数量即 TP=\\frac{n}{T\_k}=\\frac{n}{(k+n-1)\\Delta t} ，k 为流水段数， \\Delta t 为时钟周期，则当连续输入指令数 n\\rightarrow\\infty 时得到最大吞吐率 TP\_{max}=\\frac{1}{\\Delta t} 。

**流水线的加速比**：完成同一批指令，不用流水线与使用流水线所用的时间比即 S=\\frac{T\_0}{T\_k}=\\frac{kn}{(k+n-1)} ，则当连续输入指令数 n\\rightarrow\\infty 时得到最大加速比 S\_{max}=k 。

e.g.设指令由取指、分析、执行 3 个子部件完成，并且每个子部件的时间均为 Δt ，若采用**常规标量单流水线**处理机连续执行 12 条指令求用时；若采用**度为 4 的超标量**流水线处理机连续执行 20 条指令求用时。

*   已知指令由取指、分析、执行 3 个子部件完成，所以流水线的段数 k=3 。
*   采用**常规标量单流水线**处理机总用时 T=(k+n−1)×Δt=14Δt 。
*   采用**超标量流水线**处理机可以发送 4 条指令，总用时 (3+⌈(20 - 4)/4⌉)Δt=7Δt 。

e.g.设指令流水线把一条指令分为取指、分析、执行 3 部分，3 部分执行时间不等长，且 3 部分的时间分别是 t\_{取指}​=2ns ， t\_{分析}​=2ns ， t\_{执行}​=1ns ，则 100 条指令全部执行完毕求用时。

每个功能段的时间设定为三者的**最长时间 2ns**，第一条指令在第 5ns 时执行完毕，其余的 99 条指令**每隔 2ns 执行完一条**，所以 100 条指令全部执行完毕要 (5+99×2)ns=203ns。

一个 **m 段流水线在稳定运行**时的 CPU 吞吐能力，与拥有 **m 个并行处理部件**的 CPU 吞吐**能力相同**。

六、总线与 I/O 接口
------------

### 1、总线基础

（1）总线定义与分类

总线是一组由**桥接器连接交换流量**，供多个部件传送信息的线路，特点包括：

*   **分时**：同一时刻仅一个部件可向总线发送信息，**多个部件分时发送信息**。
*   **共享**：总线上可以挂接多个部件，可**分时共享线路并同时接收相同信息**。

总线按功能层次可以分为：

**片内总线​**​：芯片内部，用于 CPU 芯片内部各寄存器与 ALU 连接。

**系统总线​**​：计算机系统内各功能部件（CPU、主存、I/O 接口）间连接。

*   **数据总线 DB**：用于**双向**传输计算机系统中流动的数据和指令，宽度反映一次可处理的数据位数。
*   **地址总线 AB**：向主存或 I/O 单元**单向**传送地址信息，宽度反映了 CPU 最大寻址空间。
*   **控制总线 CB**：用于传输**控制信息和同步信号**，包括时钟信号、I/O 控制信号、中断请求等。

tips：主存通过**总线类型来识别**信息是数据还是地址，**间址寻址**首次访问内存获得的是操作数的有效地址，其作为数据**通过 DB 传输至 CPU。**​**​**

**I/O 总线​**​：通过 I/O 接口与系统总线相连，用于分离低速与高速设备，提升总线性能。

**通信总线​**​：也称电缆，计算机系统与外设或其他系统（远程通信、测试设备等）间传送信息 。

按时序控制可以分为：

*   ​**​同步总线​**​：部件/设备通过统一时钟同步，在规定时钟节拍内完成总线操作以实现信息传输。
*   ​**​异步总线​**​：部件/设备无统一时钟，以信号握手协调信息传输，总线操作时序不固定。

按数据传输方式可以分为：

*   ​**​串行总线​**​：一条双向或两条单向数据线，按比特位串行传输，效率较低，适合**长距离通信**。
*   ​**​并行总线​**​：多条双向数据线可同时传输，效率高但传输有差异且易相互干扰，适合**近距离通信**。

tips：并行总线并非总比串行总线快，并行总线因多数据位同时传输**存在协同与干扰问题**，工作频率难持续提高；串行总线可通过**提高工作频率提升速度**，最终超越并行总线速度。

*   总线设备分为主设备（**发出总线请求并获得控制权**）和从设备（**被主设备访问并响应主设备总线命令**）。
*   一条总线上**可以有多个设备作为主设备**，但**同时只能用一个主设备控制总线**传输信息。

（2）系统总线结构

​**​单总线结构​**​：CPU、主存、I/O 设备（经 I/O 接口）挂在一组总线上，可直接交换信息。优点是结构简单、成本低、易接入新设备；缺点为**带宽低负载重，不支持并发传送**。

![](https://pic3.zhimg.com/v2-60b2fd5968b39da1be392a7e92a557b4_r.jpg)

​**​双总线结构​**​：含主存总线（CPU、主存、通道间传数据）和 I/O 总线（外部设备与通道间传数据）。优点是实现存储器与 I/O 总线分离；缺点**需增加通道等硬件**。

​**​三总线结构​**​：有主存总线（CPU 和内存间传地址等）、I/O 总线（CPU 与外设通信）、DMA 总线（内存和高速外设传数据）。优点是提高 I/O 设备性能和系统吞吐量；缺点是**任意时刻只能用一种总线，工作效率低**。

![](https://picx.zhimg.com/v2-64db22dd65d37279fa3e65c09f68eb89_r.jpg)

（2）常见总线标准

**PCI**（Peripheral Component Interconnect）是一种由 Intel 于 1992 年引入的**与处理器时钟频率无关**的局部总线标准，用于将外设（如网卡、声卡、显卡等）连接到计算机的主板上，使得数据通过多个线路同时传输，且多个设备可以共享同一条总线，通过仲裁机制控制访问。即插即用，支持自动检测和配置新硬件设备。

PCI 广泛用于上世纪末与 20 世纪初的个人电脑和服务器中，但由于带宽限制，现在已逐渐被 PCIe 取代。

**PCIe**（Peripheral Component Interconnect Express）采用高速**串行**双通道传输，每个设备有独立的链路，每条链路称为**“通道”**可以提供单向上传输数据，通道数可以是 1、2、4、8、16 或更多，通常表示为“x16”、“x4”等，区别主要体现在**插槽的长度和针脚数量**上，每个通道都有一对发送和接收线路；全双工且点对点连接，支持同时发送和接收数据，还支持动态添加和移除设备。

![](https://picx.zhimg.com/v2-7176d11f93fb4a903ae90a3740caed31_r.jpg)

早期的 PCIe 1.0 起始速率为每通道 250 MB/s，如今的 **PCIe 5.0 每通道达 4 GB/s**，目前广泛应用于现代计算机系统中，包括显卡、固态硬盘 SSD、网络接口卡 NIC 等高带宽需求的硬件设备。

USB（Universal Serial Bus）是一种用于连接计算机及外围设备（如 U 盘、键盘、鼠标、存储设备、打印机等）的通用串行通信总线标准，由 USB-IF（USB Implementers Forum）推出，有以下多种类型：

![](https://pica.zhimg.com/v2-1f92d2a9da49300bcd250a3fdfe38b4e_r.jpg)

USB 总线**易使用、速度快、可靠性高、低成本低功耗可供电**，采用串行总线，数据逐位传输；支持多种拓扑结构；**即插即用**，支持**自动检测和配置新硬件设备**。不同版本有不同的速率：

*   **USB 1.1**：低速（1.5 Mbps）和全速（12 Mbps）；
*   **USB 2.0**：高速（480 Mbps）；
*   **USB 3.0/3.1**：超高速（5 Gbps/10 Gbps）；
*   **USB 3.2**：两倍速率（高达 20 Gbps）；
*   **USB 4**：基于 Thunderbolt 3 协议，带宽可达 40 Gbps。

（3）总线的性能指标

*   总线时钟周期：计算机统一时钟控制总线的时间周期。
*   总线时钟频率：时钟周期的倒数。
*   总线传输周期：一次总线操作时间，含申请、寻址、传输和结束阶段，由若干时钟周期构成。
*   信号线数**​**​：地址、数据和控制总线 3 种总线数的总和。
*   总线复用：不同信号在同一信号线上分时传输的方式，节省成本但不能提高传输率。
*   **总线工作频率**：总线上操作频率，为周期倒数。
*   **总线宽度**：即总线位宽，总线上能同时传输的数据位数，如 32 根称 32 位总线 8 字节宽度。
*   **总线带宽**：最大数据传输速率，是所有总线周期都在传数据的理想情况，用**字节/秒**衡量。

总线最主要的性能指标为后三个，**总线带宽 = 总线工作频率×总线宽度**（B/s），使用突发传输也能提高传输率。

（4）总线事务与定时

总线事务：从请求总线到完成总线使用的操作序列，是**总线周期内活动**。典型事务包括：

*   ​**​请求阶段​**​：主设备（CPU 或 DMA）发出总线传输请求，并且获得总线控制权。
*   ​**​仲裁阶段​**​：总线仲裁机构决定将下一个传输周期的总线使用权授予某个申请者。
*   ​**​寻址阶段​**​：主设备通过总线给出要访问的从设备地址及有关命令，启动从模块。
*   ​**​传输阶段​**​：主模块和从模块进行数据交换，可单向或双向进行数据传送。
*   ​**​释放阶段​**​：主模块的有关信息均从系统总线上撤除，让出总线使用权。

总线上的数据传送方式分为非突发方式和突发方式两种。

*   ​**​非突发​**​：每周期**先传地址再传数据**，主从设备常**每次只能传输一个字长**的数据。
*   ​**​突发​**​：寻址阶段发送的是连续数据单元的**首地址**，传输阶段发送**多个地址连续的数据**，每个时钟周期可以传送一个字长的信息，一组数据传完才释放总线。

总线定时：总线在双方交换数据的过程中需要**时间上配合关系的控制协议或规则**。有以下四种：

**同步定时方式**以统一时钟信号协调收发定时，时钟产生**等间隔的总线周期进行一次数据传送**。

*   **特点​**​：传送速度快、总线控制逻辑简单；但主从设备强制同步，无法及时检验数据有效性，可靠性差。
*   ​**​适用场景​**​：总线长度较短及部件存取时间接近的系统。
*   ​**​同步串行通信​**​：发送方时钟直接控制接收方时钟，仅在数据块头尾加开始和结束标记，传输效率高但硬件复杂，较少采用。

**异步定时方式**无统一时钟和固定时间间隔，依靠**握手信号一次传输多位**，主设备请求，从设备回答。

*   **特点**：总线周期长度可变，能适应速度差异大的部件设备；但较同步方式复杂、速度慢。
*   ​**​不互锁方式​**​：主设备和从设备的请求回答信号**撤销无互锁**关系，**速度最快**。
*   ​**​半互锁方式​**​：主设备撤销请求**需等从设备回答**，从设备撤销回答无需等主设备请求撤销。
*   ​**​全互锁方式​**​：主、从设备的请求回答信号撤销**均存在互锁**关系。

![](https://pic3.zhimg.com/v2-cffb4941439ab667209e370fd8fd07e4_r.jpg)

**半同步定时方式**融合同步与异步定时的优点，允许不同速度设备协同工作，**增设 Wait 响应信号线**。

*   **特点**：控制简单、可靠性高；但系统时钟频率受限，工作速度不高。
*   ​**​适用场景​**​：系统速度不高但设备速度差异大的简单系统。

**分离式定时方式**将总线事务分为请求和应答子过程，**主从设备在各自子过程中均为主设备**，单向信息流。

*   **特点**：总线利用率高，不存在空闲等待时间；但控制复杂、开销大。

### 2、I/O 基础

（1）I/O 接口与端口认识

I/O 接口（I/O 控制器）是主机与外设的信息交换界面，用于接收主机发送的 I/O 控制信号，主要功能包括**译码选址、传送命令、传送数据、信息格式转换、反映 I/O 设备状态**等。

![](https://pic4.zhimg.com/v2-d389b2f95720e941c44f04398c0c960f_r.jpg)

I/O 接口通用结构，主机侧和设备侧的数据线宽度不一定相同

*   数据缓冲寄存器用来暂存与 CPU 或内存之间传送的数据。
*   状态寄存器用来记录接口和设备的状态信息。
*   控制寄存器用来保存 CPU 对外设的控制信息。
*   **状态与控制寄存器的传送方向相反，访问时间错开**，可合二为一。

tips：主机和外设之间的连接通路是 **CPU 和主存 — I/O 总线 — I/O 接口 — 通信总线 — 外设**。

**I/O 接口即 I/O 控制器用来接收 I/O 控制信号**，常见有打印机适配器、网络控制器、可编程中断控制器等。I/O 逻辑即设备控制器，用来实现对设备的控制。

*   **按数据传送方式​**​：分并行接口和串行接口，需数据格式转换。
*   **按主机控制方式​**​：有程序查询接口、中断接口和 DMA 接口等。
*   ​**​按功能的灵活性​**​：分为可编程接口和不可编程接口。

​**​I/O 端口定义​**​：I/O 接口电路中 **CPU 可直接访问的寄存器**，包括数据端口、状态端口和控制端口，每个端口对应一个端口地址。CPU 能够对数据端口的数据做**读写操作**，但对状态端口的外设状态**仅读操作**，对控制端口的控制命令**仅写操作**。

*   ​**​独立编址​**​：即 I/O 映射方式，其**地址空间与主存地址空间独立**，范围**可重叠**，CPU 需专门的 **I/O 指令**实现 I/O 操作。译码简单、寻址快，程序清晰；但指令少，灵活性差，CPU 控制复杂。
*   ​**​统一编址​**​：即内存映射方式，**I/O 端口和主存单元在同一空间不同分段**，地址可能相同，CPU 用**访存命令**实现 I/O 操作，依靠**不同地址码区分**存储单元和 I/O 设备。访问灵活方便，端口编址空间大，保护机制可由虚拟存储管理系统实现；但占用部分主存地址空间，译码电路复杂，译码速度降低。

tips：I/O 指令是指令系统的一部分，是**机器指令**的一类，但**格式和其他通用指令相比有所不同**。其实现的数据传输通常发生在 CPU **通用寄存器和 I/O 端口**之间。

I/O 管理需要完成以下 4 部分内容：

*   **状态跟踪**：要能实时掌握外部设备的状态。
*   **设备存取**：要实现对设备的存取操作。
*   **设备分配**：在多用户环境下，负责设备的分配与回收。
*   **设备控制**：包括设备的驱动、完成和故障的中断处理。

（2）I/O 软件层次结构

系统将设备管理模块分层，各层利用下层服务完成 I/O 子功能，屏蔽细节，向上层提供服务。**只要层次接口不变，修改某层软件不影响其他层**，仅最低层涉及硬件特性。一般分为：

![](https://pic2.zhimg.com/v2-77a746ca3a96499984b369b6ee57f681_r.jpg)

从上到下顺序口诀：用户独立驱动中断硬件

将二进制整数转换成 ASCII 码打印是用户层软件通过 I/O 库函数完成的。

设备独立性软件层是指**用户程序与实际使用物理设备无关**，且对各种设备都要进行管理，如**缓冲区管理、磁盘高速缓存、检查权限设备保护**等。

*   通过**逻辑设备表 LUT**（Logical Unit Table）来建立**逻辑设备名到物理设备名的映射关系**。
*   应用程序**按逻辑设备名访问设备**，经驱动程序的处理来控制物理设备，灵活易于 I/O 重定向。
*   若更换物理设备则**只需更新驱动程序**，无需修改应用程序。

![](https://pic4.zhimg.com/v2-a83fb2505f2216d318b27f8eb1b214bf_r.jpg)

单用户系统整个系统一张 LUT，多用户系统每个用户一张 LUT

设备驱动程序层管理 I/O 设备，由**汇编+高级**语言编写，**直接相关硬件**，如磁盘调度等，该层负责与设备控制器传递信息，执行操作系统发出的 I/O 指令，**每类设备只需一个**，常固化在 ROM 中。

*   ​**​指令转换​**​：接收上层抽象 I/O 请求，转化为设备具体要求，如**将盘块号转为磁盘具体参数**。
*   ​**​请求核查​**​：检查用户 I/O 请求合法性，再检查设备状态，**传递操作参数，设置工作方式**。
*   ​**​操作调度​**​：设备空闲时立即启动执行 I/O 操作；设备忙时**将请求者 PCB 挂到设备队列等待**。
*   ​**​中断响应​**​：及时响应设备控制器中断请求，**调用对应不同的中断处理程序**。

中断处理程序进行**进程上下文的切换**，对处理中断信号源进行测试，**读取设备状态和修改进程状态**等，与硬件紧密相关，应尽量屏蔽，放在操作系统底层。

tips：对设备驱动程序的处理过程顺序为将抽象要求转化为具体要求、对服务请求进行校验​​、检查设备的状态、传送必要的参数​​并最后启动 I/O 设备执行操作。

（3）I/O 应用程序接口

![](https://pic2.zhimg.com/v2-2b753efb6629778c8990dd55bd08f269_r.jpg)

输入/输出应用程序接口

操作系统的 I/O 接口还涉及两种模式：

**阻塞 I/O**：用户进程调用 I/O 操作时进程被阻塞，移到阻塞队列，操作完成后唤醒移到就绪队列。优点是操作简单、实现难度低，适合**并发量小**的应用开发；缺点是 **I/O 执行阶段进程一直阻塞**。

**非阻塞 I/O**：用户进程调用 I/O 操作时不阻塞进程，进程需不断查询操作是否完成，完成后系统将数据从内核复制到用户空间。优点是等待期间进程可做其他事，适合**并发量大**的应用开发；缺点是**轮询询问结果会占用 CPU 时间**。

（4）I/O 方式详解

输入/输出系统实现主机与 I/O 设备数据传送，有程序查询、程序中断、DMA等常用 I/O 方式。

**程序查询方式**：信息交换由 CPU 执行程序控制，其接口设有数据缓冲寄存器和设备状态寄存器，主机 I/O 操作时先读设备状态以决定后续操作，每完成一次数据传输后**主存地址+1，计数值-1**。工作流程如下：

![](https://pic4.zhimg.com/v2-05877b8e3f86a212fcca400a765c5841_r.jpg)

设计简单，用户进程不会被阻塞，但 CPU 花费较多时间在查询和等待，且与设备串行工作效率低

*   ​**​独占查询​**​：设备启动后，CPU 持续查询接口状态完全用于 I/O，**外设与 CPU 串行工作**。
*   ​**​定时查询​**​：CPU 周期性查询接口状态，条件满足才传数据，完成后返回到用户程序，时间间隔与设备数据传输速率相关。

**程序中断方式**：进程需 I/O 操作时启动外设阻塞自身，CPU 选其他进程执行，外设准备就绪发中断请求，CPU 响应后**执行中断服务程序完成数据传送**，再返回原进程继续执行，实现**外设与 CPU 并行工作**，传送与主程序仍是串行的。**外设准备数据的时间应大于中断处理时间**。

![](https://pica.zhimg.com/v2-50c0444bb774abb2e3ba07df2e86b08c_r.jpg)

中断服务程序属于操作系统模块，以供调用执行，适合字符型设备不适合高速外设

**DMA 方式**：依靠**硬件电路**实现，主存和 I/O 设备之间有直接数据通路，**无需 CPU**，一次 DMA 传送外设和主机之间**一个数据块**。**CPU 与外设、传送与主程序均并行工作**速度快效率高，**适用于需要大量数据高速传输**的设备，如磁盘、显卡、固态硬盘、网卡等。

![](https://picx.zhimg.com/v2-2c3e0c6f030a1608703c15102a4d6d1b_r.jpg)

DMA 预处理和后处理都在内核态需 CPU 参与，分别负责请求 I/O 和中断服务程序

DMA 控制器的主要功能如下：

*   **DMA 请求**：接收外设的 DMA请求，并向 CPU 发出请求。**具备 DMA 接口才能产生**。
*   **总线控制**：CPU 等一个**总线周期结束**后再响应，**DMA 接管总线控制权**。
*   **地址和计数**：硬件电路确定传输的起始地址、长度，**自动修改其计数**。
*   **数据传输**：按规定方向**发送读写等控制信号**，在**设备接口与主存之间**执行数据传输。
*   **操作结束**：向 CPU 报告 DMA 操作结束。

![](https://pic2.zhimg.com/v2-2ec358282727905f3f016c123925604d_r.jpg)

*   **主存地址计数器**存储数据交换的主存地址，**每传输一个字，地址 +1**，直到数据传输完成。
*   **传送长度计数器**记录数据总长度，**每传输一个字，计数器 -1**，直到传输完毕。
*   **数据缓冲寄存器**暂存每次传输的数据，与**主存之间传输单位通常为字**，与**设备之间可能为字节或位**。
*   **DMA 请求触发器**待 I/O 设备准备好数据后发出控制信号，触发 DMA 请求。
*   **控制/状态逻辑**指定传输方向，修改参数，协调 DMA 请求与 CPU 响应。
*   **中断机构**在数据传输完成后向 CPU 请求中断，DMA 传输期间直接接管总线，结束后恢复 CPU 控制。

DMA 方式和程序中断方式的主要区别：

*   **程序切换**：中断方式需要**程序切换和现场保护**，DMA 方式**不中断程序无需保护现场**。
*   **响应时机**：中断请求的响应只能在**指令执行结束后**，DMA 请求的响应可以在**任意机器周期结束时**。
*   **CPU干预**：中断传送需要 CPU 干预，DMA 传送不需要 CPU 干预数据传输速率高。
*   **优先级**：DMA 请求的**优先级高于中断请求**。
*   **处理能力**：中断方式能处理异常事件，DMA 方式主要用于大批量数据传送。
*   **数据传送**：中断方式靠**软件传送**，DMA 方式靠**硬件传送**。

tips：程序中断的中断请求完全是为了**传输数据**，DMA 方式的中断请求是为了**报告 CPU 数据传输结束**，两种中断 I/O 方式都会让**进程阻塞**。

为避免 I/O 设备和 CPU 同时访问主存发生冲突，DMA 和 CPU 通常采用以下方式：

**停止 CPU 访存**：当 I/O 设备请求 DMA 时，DMA 接口向 CPU 发送停止信号，CPU 放弃总线控制权，DMA 传送数据，数据传送结束后，DMA 通知 CPU 恢复总线控制权。

![](https://pic1.zhimg.com/v2-11c73d8022b686d1017c072fcb76cae6_r.jpg)

控制简单适合高速数据传输，但 CPU 在访存时基本处于不工作状态

**周期挪用**：CPU 不在访存 I/O 可**立即传送通过 DMA 传送数据**；CPU 正在访存则**等待周期结束后再让出总线占有权**；I/O 和 CPU 同时请求访存时 CPU **暂时放弃总线**。

![](https://pic4.zhimg.com/v2-d314fd0cbd8f4a9cc659efe93a832487_r.jpg)

效率高但每挪用一个存取周期需申请、建立和归还总线控制权

**DMA 与 CPU 交替访存**：将 CPU 工作周期分成两个时间段，轮流分配给 CPU 和 DMA。

![](https://pic1.zhimg.com/v2-59cac60634bf41bada909e0456cfcc60_r.jpg)

不需要总线控制权的申请、建立和归还过程，传送速率高，但硬件逻辑较复杂

（5）设备的分配与回收

系统位每台设备确定一个**绝对号以便区分和识别设备**，设备分配指根据用户的 I/O 请求分配所需的设备，充分发挥设备使用效率，避免进程死锁。主要数据结构有：

*   **系统设备表 SDT**：系统唯一，记录所有设备的情况，每个物理设备对应一个表项（设备类型、设备标识符、DCT、驱动程序入口）。​
*   **设备控制表 DCT**：每个设备对应一张，包含设备类型、标识符、设备状态、指向 COCT 的指针、设备队列指针等字段，设备释放后状态改为空闲实现回收。
*   **控制器控制表 COCT**：每个控制器对应一张，包含控制器状态、标识符、指向 CHCT 的指针、控制器队列指针等字段。
*   **通道控制表 CHCT**：每个通道对应一张，包含通道状态、标识符、通道队列指针等字段，管理连接的多个设备控制器。

tips：按**上述顺序**获取信息，仅**设备、控制器、通道三者都分配成功**了才算设备分配完整成功。​**​**

**设备固有属性​**

*   独占设备：静态分配，**进程独占直至释放**，如打印机、扫描仪。
*   共享设备：动态分配，一个时间间隔内**可被多个进程访问**，但在​**​同一时刻​**​**仅一个进程**真正占用设备，**可寻址和随机访问**，如磁盘。
*   虚拟设备：**可共享的可虚拟化设备**，如 SPOOLing 将独占设备改造为共享设备。

tips：当用户使用打印机打印某文件时，需要**通过文件系统来访问磁盘上的数据**，然后通过**设备管理程序来控制打印机的输出**。打印机启动程序只能初始化打印机，不能直接访问磁盘上的数据。

**设备分配方式**

*   **安全分配方式​**​：进程发出 I/O 请求后进入阻塞态，不能请求和保持资源，直到 I/O 操作完成后才唤醒。CPU 和 I/O 设备串行工作，效率较低。
*   ​**​不安全分配方式​**​：进程发出 I/O 请求后继续运行，需要时可发出多个 I/O 请求，仅当请求的设备被其他进程占用时才进入阻塞态。可能造成死锁。

设备分配考虑设备**固有属性、独立性和安全性**，算法一般采用 **FCFS** 或**最高优先级优先分配**算法。

（6）SPOOLing 技术

为缓和 CPU 高速性与 I/O 设备低速性的矛盾，引入 SPOOLing **假脱机技术将**将一台物理设备虚拟为多台逻辑设备，建立在**多道程序设计技术的基础**上，**提高独占设备的利用率**。

*   ​**​输入/输出井​**​：在**磁盘**上开辟的两个存储区域。输入井接收 I/O 设备输入数据；输出井存放用户程序输出数据，**每个进程的 I/O 数据以文件形式链接成队列**。
*   ​**​输入/输出缓冲区​**​：在**内存**中开辟的两个缓冲区。输入缓冲区暂存输入设备送来的数据，再传至输入井；输出缓冲区暂存输出井送来的数据，再传至输出设备。
*   ​**​输入/输出进程​**​：输入进程模拟脱机输入的外围控制机，将数据从输入设备传至输入缓冲区，再存入输入井，CPU 需要时直接从输入井读入内存；输出进程模拟脱机输出的外围控制机，可**随时将数据从输出井传至输出缓冲区**，等输出设备空闲再输出到输出设备，**不必等待 I/O 完成**。

这三部分由**预输入**程序、**井管理**程序和**缓输出**程序管理，保证系统正常运行。

![](https://pica.zhimg.com/v2-009ca8f44d09156c81cee494907fc634_r.jpg)

SPOOLing 技术需要磁盘和内存空间，但不需要外围计算机的支持，属于空间换时间的技术

tips：**通道属于硬件技术**，SPOOLing、缓冲池、内存覆盖都是在内存基础上用**软件**实现。

（7）I/O 操作实例

以 C 语言的 scanf 函数为例分析：

![](https://pic3.zhimg.com/v2-7e8fb499513d9d79978addfeb7359912_r.jpg)

在 scanf 函数执行的第一阶段申请了用户缓冲区 buf，检查与 scanf 函数关联的用户缓冲区 buf，若缓冲区中已有数据，则直接读取。若缓冲区为空，则触发系统调用 read，以从内核缓冲区中读取数据。

![](https://pic1.zhimg.com/v2-a2fbf3e1b322e21b730296da68e40e1e_r.jpg)

整个过程的数据流向

进程 P 通过**系统调用接收键盘字符**的完整过程如下：

1.  **将进程 P 插入阻塞队列**：进程 P 执行 “键盘输入” 系统调用时，由于字符尚未输入，操作系统会将 P 从运行队列移除，插入阻塞队列（等待 I/O 完成）。
2.  **用户在键盘上输入字符**：用户按下键盘按键，触发键盘硬件产生中断信号。
3.  **启动键盘中断处理程序**：硬件中断信号触发 CPU 切换到内核态，操作系统启动键盘中断处理程序处理。
4.  **将字符读入系统缓冲区**：中断处理程序从键盘控制器中读取输入的字符暂存到操作系统的系统缓冲区中。
5.  **将进程 P 插入就绪队列**：字符读取完成后，操作系统将进程 P 唤醒，插入就绪队列，等待 CPU 调度。
6.  **进程 P 从系统调用返回**：当进程 P 被调度执行时，从系统调用的断点处继续执行，获取系统缓冲区中的字符，最终从系统调用返回用户态，继续执行用户程序。

tips：键盘中断服务例程运行在内核态，先**从键盘控制器的数据寄存器读取**输入数据，例程执行完后将数据**暂存到内核缓冲区**；后续再由内核将数据**传递到用户缓冲区**。

文章

\>由 \[Circle 阅读助手\](https://circlereader.com) 生成